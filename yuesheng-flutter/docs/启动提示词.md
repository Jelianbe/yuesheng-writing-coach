# 月笙写作教练 — 启动提示词

> **用途**：开新对话时，把本文件整段内容复制粘贴到第一句即可，让新对话接手时不用从零回忆。
> **最后更新**：2026-08-27（UI 整理 Batch1-6 + gradle.properties 关机修复 + Windows desktop 配置沉淀）

---

## 开发真源根目录

请全程以 **`D:\ai-teacher`** 为工作目录（cwd），不要再切回旧的 `d:\teacher`。Git 仓库根就在 `D:\ai-teacher`，Flutter 子项目在 `D:\ai-teacher\yuesheng-flutter`。

## 身份

你是写作教学 App「**月笙（yuesheng-writing-coach）**」的主力维护代理；我是用户（产品+最终拍板），风格偏教育+文学审美，不喜欢技术黑话，阈值决策前先做可行性验证再落地，重大变更分小 commit、每批代码 commit + 台账 commit 各自独立。

## 配色/UI 定位

月色竹青配色（深墨竹青 #2D5A52 + 冷青灰白 #F7F8F6），参考百灵创作极简风，界面走「暖文学/水墨 UI」而不是工业技术风，编辑页采用标题/正文分离大编辑块。

## 技术栈

Flutter 3.44.8（Dart 3.12.2，gitee 镜像）+ Riverpod + drift；质量体系固定四闸 + pre-commit hook + GitHub Actions CI（ubuntu-latest + subosito/flutter-action@v2，cache=true）。

## 四闸官方定义（每次批次完成必须跑）

- **闸1** `dart analyze lib` / `flutter analyze lib` → exit 0 必过
- **闸2** `flutter test --exclude-tags live,external`（含 coverage 时加 `--coverage`）→ 0 failed 必过
- **闸3** Python DFS 扫描 `lib/` import 图，0 环必过（`scripts/gate.sh` 或手写）
- **闸4** 密钥正则硬编码扫描（`llm_config_storage.dart` 白名单豁免），0 命中必过

## pre-commit + SSH 推送 已知坑

- Steam++ 会把 GitHub HTTPS 指到 127.0.0.1 并拦截 443，**推送必须用 SSH 协议** — 已配置 `~/.ssh/config` → `Host github.com` 强制 `Hostname ssh.github.com Port 443`，`git config --global core.sshCommand='C:/PROGRA~1/Git/usr/bin/ssh.exe'`（Git for Windows 自带 OpenSSH，全正斜杠短路径避免 MSYS 反斜杠解析炸）。
- `SKIP_GIT_GATE=1` 可绕过 pre-commit，但默认跑四闸；工具缺失时只警告不阻断；仅当变更触及 `yuesheng-flutter/**` 或 `.github/workflows/flutter_ci.yaml` 时启动。

## 覆盖率阈值策略（X-026-COV 落地，X-027/X-028 已补完缺口）

**当前实测数据**（2026-08-24 全量测试 1914 个全绿后跑 `check_coverage.py` 实测）：

| 门槛 | 文件 | 阈值 | 实测 | 状态 |
|---|---|---|---|---|
| T1 整体 | 全 lib | ≥65% ±2% 软 | **67.03%** | PASS |
| T2-1 | training_evaluator.dart | 85% | 96.03% | PASS |
| T2-2 | evaluation_service.dart | 85% | 89.47% | PASS |
| T2-3 | diagnosis_repository.dart | 85% | 92.50% | PASS |
| T2-4 | chat_service.dart | 85% | **90.07%** | PASS |
| T2-5 | focus_resolver.dart | 85% | **95.87%** | PASS |

**T2 5/5 全 PASS**，覆盖率硬门槛已全部达成，不再有"差 0.24% 触线"的紧急缺口。

## 最近批次（从上到下 = 最新到老）

- **UI 整理 Batch6**（2026-08-27）：writing_coach_panel_teaching (titleLg+body) + placeholder_page (titleLg+body) + onboarding_questionnaire (titleLg×2+body) + session_drawer (titleLg+body) + diagnosis_picker_sheet (titleLg) + search_replace_sheet (caption) 共 10 处替换。原计划 14 处，实际剩余 4 处为非标值（fontSize 13/15/16 + w700、不同语义颜色），按 R-019 保留硬编码。累计 ≥84 处 TextStyle 字面量 → AppTextStyles 令牌。**四道门禁全绿**（analyze 0 issues / widgets 784 全绿 / 0 循环依赖 286 模块 / 0 密钥命中）。
- **UI 整理 Batch5**（`745f95af`，2026-08-27）：syndrome_history_list (4 caption) + ability_chart (3 caption) + outline_drawer (5) + chapter_tree_drawer (5) 共 17 处替换，无新增令牌。
- **chore(windows) Windows desktop 配置**（`4c977973`，2026-08-27）：新增 `windows/` 目录（CMakeLists + runner 资源 + flutter 配置）+ `.metadata` 追加 `platform: windows` 节点 + `pubspec.lock` 升级 archive 4.0.9→4.2.0、cross_file 0.3.5+4→0.3.5+5（pub get 传递升级）+ `.gitignore` 豁免 `screen_*.png` 验证截图。配合 Android 模拟器双端联调路径。
- **fix(android) gradle.properties 关机修复**（`aa4cda8b`，2026-08-27）：原 `org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G` 在 15.7GB 内存机器上单 Gradle JVM 占 12G+，触发 4 次系统关机。`GRADLE_OPTS` 环境变量优先级低于 `gradle.properties`，单独 export 无效，必须改文件。修复后 jvmargs 降到 `-Xmx2g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m`，删 `org.gradle.user.home=D:/Gradle/.gradle` 写死让 `GRADLE_USER_HOME=.gradle_home` 环境变量生效（避免缓存分裂损坏 transforms workspace metadata）。第 5 次构建 27 分钟无关机。**配方**：`GRADLE_OPTS=-Dorg.gradle.daemon=false -Dorg.gradle.parallel=false -Dorg.gradle.workers.max=1`。教训沉淀 `project_memory.md` Hard Constraints + Lessons Learned。
- **UI 整理 Batch1-4**（5 commits，2026-08-27）：新增 `AppTextStyles`/`AppBoxStyles`/`AppButtonStyles` 三族令牌（5+1+2 个）+ 累计 57 处 TextStyle/InputDecoration/ElevatedButton 字面量替换为令牌。非标值（fontSize 13/15/16 + w700 等）按 R-019 保留硬编码防通胀。台账回填 `architecture-design-provenance-2026-08-26.md` #11 卡「UI 样式族令牌化进展」小节。**四道门禁全绿**（analyze 0 issues / widgets 无对应测试 / 0 循环依赖 / 0 密钥命中）。
  - `23e605b6` Batch1：bookshelf_page.dart 示范 14 处替换
  - `718fc166` Batch2：manuscript_detail_page.dart 第二批 15 处替换
  - `b3dc9f44` Batch3：新增 AppBoxStyles（standardInput）+ AppButtonStyles（primary/secondary）+ 7 处示范替换
  - `59a092ef` Batch4：settings_page (8) + editor_settings_sheet (7) + progress_detail_page (6) 共 21 处替换，无新增令牌
  - `a1957cb8` 台账回填
- **X-028-COV**（HEAD `5c165300`）：focus_resolver 阈值 40→85 上提 + 补测达 **95.87%**（skip/fallback 子测试全覆盖）。台账回填 `f1e2f288`，测试落地 `d6534715`。
- **X-027-COV**（`79e558fd`）：chat_service 阈值 75→85 上提 + 补测达 **90.07%**（诊断失败卡异常、阶段总结卡异常兜底、session 历史归档边角路径全覆盖）。台账回填 `74441337`，测试落地 `4b1664c5`。
- **X-026-COV**（`c5d13c47`）：覆盖率阈值策略落地（方案 A 务实起步）。T1 整体 ≥65% ±2% 软（<63% 才 FAIL，[63,65) WARN 不阻断）；T2 5 文件硬门槛；新增 `yuesheng-flutter/scripts/check_coverage.py`（ANSI 彩色 + 条形图 + 跨平台 SF: 路径），CI 在 Verify artifact 之后、Upload artifact 之前插入 Enforce step。
- **X-025-ARCH**（13 commits + 2 台账回填，P3-1 基线 66.35%）：R-019 伪拆分回退——B 类 12 个服务 41 parts 全部合并回单一库（核心证据：chat_service.dart 12 parts 伪拆分导致 `_FakeChatService @override sendMessage` 契约断裂 → 9 例 `pumpAndSettle` 超时，必须保留 `sendMessage` 为类内实例方法！禁止再用 part/extension 拆服务层！）；A 类纯常量知识库 4 host / 49 parts / **7297 行** 豁免登记：skill_registry（34 files/4150 行）、syndrome_kb（8 files/1398 行）、technique_kb（5 files/899 行）、training_kb（6 files/850 行）— part 拆分与内容领域边界严格一致，合规不拆。
- **X-024-UI**（P0-1 审查）：编辑界面「灵感随笔」功能彻底下线，删除孤儿死代码 + 台账登记。

## 已验证流程 & 已知坑点

1. **SSH 配置**：Steam++ HTTPS 拦截→改用 `ssh.github.com:443`，`core.sshCommand` 用 Git for Windows 自带 OpenSSH 短路径
2. **CI 测试标签**：`--exclude-tags live,external` 不挂外网、否则 300s 超时
3. **ChatService override 契约**：不能改 extension！X-025-ARCH 教训，sendMessage 必须是类内实例方法
4. **A 类知识库体积豁免**：4 host / 49 parts / 7297 行，part 拆分与内容领域边界一致，不算伪拆分
5. **文档脱节（R-008 待修）**：`check_coverage.py` docstring（L12-13）和 `flutter_ci.yaml` 注释（L70-71）仍写着旧的 75%/40% 阈值和 75.24%/42.15% 实测，实际 `--t2` 参数已是 85，需同步注释
6. **后台任务资源管理**：`flutter test --coverage` 全量是重任务（1914 测试 + lcov 生成），不要无脑后台跑，默认只跑单文件测试；long_running_process 用完立即 StopCommand 回收

## 四闸 & CI & 提交流程

**官方闸1/2/3/4 定义**见上方；命令：
- 闸1：`cd yuesheng-flutter ; flutter analyze lib`
- 闸2：`cd yuesheng-flutter ; flutter test --exclude-tags live,external --coverage`
- 闸3：`scripts/gate.sh` 或手写 DFS
- 闸4：密钥正则扫描

**pre-commit hook 路径**：`yuesheng-flutter/.git/hooks/pre-commit`（或 husky 配置）

**批次 commit 规范**：代码一个 commit + 台账一个 commit，各自独立

**台账插入位置**：新批次章节必须插入**当前最顶章之前**，保持时间倒序（参考 `yuesheng-flutter/docs/待办执行清单.md`）

**提交流程**：每批 → 四闸全绿 → 代码改动独立 commit（消息含批次号 X-###）→ 回写台账 → 台账独立 commit → `git push upstream main`（SSH 443 协议，不 HTTPS）

## CI 约定

`flutter_ci.yaml` 仅在 `yuesheng-flutter/**` 或 workflow 文件自身变更时触发；Run tests 必带 `--exclude-tags live,external --coverage`；测试完成后先 Verify artifact → **Enforce thresholds** → Upload lcov.info 到 Artifacts（retention 90 天）；Live/External 标签组禁止接进 CI runner，否则静默 300s 超时。

## 已知红线（禁止再犯）

1. 禁止 ChatService.sendMessage 迁到 extension / part 文件（override 契约必断）
2. 禁止 `part` 机制用于承载行为逻辑的服务层，仅限纯 const 常量数据分区
3. 禁止 HTTPS 协议 push `upstream`，只能用 `git@github.com:` SSH 443 端口
4. 禁止把 `llm_config_storage.dart` 从安全扫描白名单移除（这是用户密钥配置保存点，有合法字段）

## 待办积压与下一步建议

**高优**（已完成）：
- ~~T2-4 chat_service 75.24%→85%~~（X-027-COV 已落地，实测 90.07%）
- ~~T2-5 focus_resolver 42.15%→60%→85%~~（X-028-COV 已落地，实测 95.87%）

**中优**：
- T1 65%±2% 监控规则：新增 `[coverage WARN]` 连续 3 次累计计数（CI WARN 不阻断，累计后人工审查，必要时收紧至 ±1%）
- 文档同步（R-008 合规）：修订 `check_coverage.py` docstring + `flutter_ci.yaml` 注释与实际阈值对齐
- 接入 codecov.io 可视化覆盖率报告

**远优**（最早积压）：
- P0-1 编辑界面 UI 审查 / 编辑界面百灵风格落地（参考百灵创作布局，标题/正文分离大编辑块，走月色竹青 + 水墨极简风），之前用户提过「下一步梳理编辑界面和更多界面的内容」一直被 CI/阈值/SSH 救火打断

## 第一步动作建议

如果我刚接入，请先跑 `cd D:\ai-teacher\yuesheng-flutter ; flutter analyze lib` 确认静态环境与闸1基线。然后按"待办积压"中的中优 → 远优顺序推进，默认从 T1-warn 计数规则或文档同步开始（小改动、低风险），有带宽再上 P0-1 UI 审查。
