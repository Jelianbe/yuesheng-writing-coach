# 交接 · 2026-09-04 后续任务（新对话开工提示词）

> 用法：把下面「二、提示词正文」整段复制到新对话，AI 读完即可接续工作。
> 本文件是台账 `docs/待办执行清单.md` §R-019 清偿进度 → §后续批次任务清单 的配套件，
> 台账是真源；本文件是自包含快照，两者冲突时以台账为准。

---

## 一、为什么需要这份东西

本轮（2026-09-04）从 07:45 到 11:00 连续推进，中途切过会话导致上下文丢失。
R-019 债务清偿是**多批次、跨会话**的长期活，每批的判据、坑、纪律散落在
`AGENTS.md` 的 V4.4–V4.20 里。没有交接件，新会话要么重复摸索，要么踩同一个坑。

---

## 二、提示词正文（复制以下内容）

```
你是「月笙写作教练」Flutter 项目的接手工。这是跨会话交接，你没有上一轮的上下文，
请先按本节核对状态再动手。所有判断必须基于可验证输入（file:line 或命令输出），
区分事实与推断。

【一、项目坐标】
- git 仓库根：D:\ai-teacher（注意：Flutter 工程在子目录，AGENTS.md 在仓库根）
- 唯一活跃真源：D:\ai-teacher\yuesheng-flutter
- 权威纪律：D:\ai-teacher\AGENTS.md（**必读**，V4.4–V4.20 是踩坑实录，逐条看）
- 任务真源：yuesheng-flutter/docs/待办执行清单.md
  → §「R-019 函数行数债务清偿进度」
  → §「安全区覆盖摸底」
  → §「后续批次任务清单（2026-09-04 交接）」
- 交接快照：yuesheng-flutter/docs/交接-2026-09-04-后续任务.md
- 门禁脚本：yuesheng-flutter/scripts/gate.sh（六道）

【二、当前状态（2026-09-04 11:00 核对点，动手前先自己验一遍）】
- main 与 upstream/main 完全同步（0 领先 / 0 落后），最近提交 27375b28
- R-019 基线 tool/r019_baseline.json = 222 条
  （止血模式：存量豁免、只卡新增；生成代码 .g.dart 已排除，永不可清偿）
- 已清偿 15 个；51–60 行安全区剩 38 个（服务层 8 个 + UI 约 30 个）
- 全量测试规模参考：2312 passed / 14 skipped
- 关键工具（都在 yuesheng-flutter/tool/ 与 scripts/）：
  · check_r019.py                 行数扫描（--baseline 止血模式）
  · _verify_r019_baseline.py      V4.14 基线收紧后验证仍能拦（参数化）
  · _survey_r019_coverage.py      安全区覆盖摸底（判据级变异体检，可重复跑）
  · _verify_profile_r019_batch.py 画像家族变异验证（含基线健康校验）
  · scripts/skill_consistency_scan.py  prompt copy-drift 扫描（advisory，非硬门禁）

【三、接下来按此顺序做】
批次 F —— 直接重构（已确认真覆盖，有护栏兜底）
  1. lib/services/syndrome_skill_levels.dart:93  interventionLevelForTrainingCount 51
  2. lib/services/genui_validator.dart:33        validateGenuiComponent 51
  ⚠ 开工前先重跑 tool/_survey_r019_coverage.py 复核这两项仍为真覆盖
    （台账里那张表是 2026-09-04 的快照，代码动过就要重新确认）

批次 G —— 补测试（不是为了重构，是本来就该有的护栏）
  3. lib/services/fact_validator.dart:183   _parseEvents
     cause_event_name / causeEventName 双键兼容分支（批次3-D4）从未被任何用例走过，
     现有用例只用蛇形键 → 补驼峰键用例
  4. lib/services/training_evaluator.dart:518  buildEvaluationSummary
     totalCount = 0 的除零保护。改坏会产出 NaN 百分比并进 prompt，属输出污染级隐患

批次 H —— 先补判据边界测试，再拆结构
  5. lib/data/repositories/diagnosis_repository.dart:590  _updateDiagnosisSummary
     缺「active > 2 时 top_syndromes 截断到前 3」的场景
  6. lib/services/training_evaluator.dart:222   detectDeterioration
     缺「连续失败恰好 1 次」边界（门槛是 >= 2）
  7. lib/services/teacher_validator.dart:404    checkTeacherConsistency
     缺「仅 warn 级违规、无 error」时的通过判定
  8. lib/services/progressive_diagnosis.dart:87 splitContent
     缺「长度恰等于 kDiagnosisChunkSize」的边界

【四、待裁决（未裁决前保持原行为，不得擅自改）】
- student_profile.dart 的 _resolveStyleAndConfidence：intuitive 分支复用 analytical
  的置信度常量。查 lib/config/shared_constants.dart 的 CognitiveStyleThresholds，
  里面**没有** intuitive 专用项（只有 analytical / mixed 两套）。
  可能是有意对称设计，也可能是当初漏定义后复制了一份。已在代码注释标注。

【五、开工前必读的坑（AGENTS.md 全文都要读，以下最高频）】
- V4.18（最坑，会浪费你一小时）：跑 flutter test 前**必须清空会话代理**，
  否则所有测试集体报 "Unable to connect to flutter_tester process:
  WebSocketException: Invalid WebSocket upgrade request"——包括上一轮刚跑全绿的。
  跑法：
    env HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= \
        NO_PROXY=localhost,127.0.0.1 flutter test <文件>
  gate.sh 同理。这个故障格外阴险：加载失败的输出行**同样带 [E]**，会让变异验证
  的「拦截成功」判据假绿。故验证脚本必须先做基线健康校验（不绿就 abort）。
- V4.20（本轮最重要的教训）：**测试全绿 ≠ 覆盖存在**。零直接引用或低引用的函数，
  间接调用往往是虚覆盖。实证：student_profile 家族三个函数间接测试全绿，
  但 6 个变异全部漏网——那些判据分支从来没被任何断言锚定过。
  动手前问：「改坏这一行，哪个断言会红？」答不上来就是虚覆盖，先补测。
- V4.14：清偿后**必须重生成基线**，并逐函数验证新基线仍能拦
  （_verify_r019_baseline.py）。踩过的坑：锚点不唯一会注入到同名函数里、
  pad 不够撑不过阈值、锚点行尾要匹配（脚本已做行尾自适应）。
- V4.10：改源码做验证的脚本必须 try/finally 恢复 + 收尾 git diff 复核。
- V4.17：dart format **不带 -o none** 会写入并把整个文件重置为 LF。
  gate.sh 已带 -o none（只检查不写入），但手动跑要小心。
- V4.15 / V4.16：新护栏必须验证「能失败」；变异漏网先分清是测试失灵还是变异本身
  等价（等价变异任何测试都拦不住，别硬凑）。
  判据不能用「用例名出现在输出里」——通过和失败都会打印用例名，恒为真。
- V4.19：锚点快照 len 是 UTF-16 code units（Dart String.length），不是 UTF-8 字节；
  按 diff 行求和还会漏掉换行符。核算漂移要直接比源字符串长度。
- 锚点快照重生成（改了 prompt 才需要，且要逐 case 归因）：
    UPDATE_SNAPSHOTS=true flutter test test/services/skill_prompt_anchor_test.dart
  teaching-strategy 属 L1 常驻 → 改动会让 13 个 prompt case **等量**漂移，
  非等量即越界，停下排查。

【六、每批收尾（照做，别省）】
1. 六道门禁全绿（scripts/gate.sh，记得清代理）
2. 基线重生成 + V4.14 逐函数验证仍能拦
3. 分 commit：测试单独一个、代码+基线一个、文档一个
4. 台账同步（R-019 进度表 + 已清偿清单）
5. 推送**必须先问舰长**，且按逻辑主题分批推（本轮推了 7 批）

【七、舰长的工作偏好（照这个来）】
- 极简回复，不要套话、不要固定模板、不要罗列显而易见的东西
- **先侦察出报告，不直接改代码**（尤其 Skill 内容、裁决类工作）
- 未决策项按你推荐的来，但必须说明理由和取舍
- 破坏性操作（尤其删除）必须先报告确认，先隔离再处理——对删除高度敏感
- 不在修改范围内的测试只做抽测，别顺手全跑
- 需要 file:line 引用，区分事实与推断，严谨事实核查而非附和
- 核心模块（诊断 / Skill 注入 / 教学状态机 / DB schema）改动前**必须先写 ADR**
- 中文沟通

【八、开工第一步】
先只读核对：git log -3、git status、python tool/check_r019.py --baseline
tool/r019_baseline.json，确认与上面「当前状态」一致。不一致先报告差异再动手。
```

---

## 三、本轮成果索引（便于新会话快速定位）

| 提交 | 内容 |
|---|---|
| `27375b28` | 安全区覆盖摸底（8 函数判据级体检，2 真空 / 6 虚覆盖） |
| `20d849fb` | V4.20 教训 + R-019 进度（基线 225 → 222，已清 15） |
| `b9c1bd80` | P1 第五批：3 个画像推断函数清偿（51→23 / 56→16 / 56→23） |
| `c105840c` | 补 student_profile 家族 15 例直接单测（原零直接引用） |
| `430da299` | AGENTS.md V4.18（会话代理劫持 flutter test）/ V4.19（漂移核算单位） |
| `aa417178` | ADR-C72 实施（prompt 三处例外 + 护栏，变异 4/4 拦截） |
| `c0847905` | ADR-C72 裁决文档（N-1 零基础最小激活块） |

更早的 ADR-C70（解 3 个循环依赖、门禁 3 转全量卡口）、ADR-C71（跨会话教学状态延续）
见 `git log`。
