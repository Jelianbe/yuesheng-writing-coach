# Skill 一致性家族扫描 —— 52 条候选裁决表

> 生成：2026-09-01（接 A.12.21 首跑与校准报告）
> 输入：`_scan/skill_consistency_scan_2026-09-01.txt`（52 条候选）
> 方法：全部候选逐条回原文核实（检查项⑧），上下文窗口 ±3 行，file:line 可溯。
> 性质：**裁决报告，未动任何代码**。修复批次见文末，待用户确认后执行。

---

## 一、裁决总览

| 裁决类别 | 条数 | 处置 |
|:--|:--:|:--|
| 真冲突·已知（N 系列存量） | 9 | 维持第 7/8 批决策线，不重复立项 |
| **真冲突·新发现** | **3** | 新登记 C67 / C68（见二） |
| 同值/近似副本（V-05 治理面） | 5 处 | 并入 C68 副本登记清单 |
| 有意分层（不同对象/不同机制） | 8 组 | 无需修改 |
| 良性误聚（主题词误配，非冲突） | 27 组 | 扫描器已记录，后续跑只作背景噪音 |

52 条 = enum-doc 2 + enum-token 7 + threshold 35 组 + copy-drift 4 + ref-integrity 0 + schema-field 0 + conflict-pair 4。

---

## 二、新发现（3 项，登记 C67 / C68）

### C67｜C51/C52 引用分叉：beginner_p6:130 空引用（P2）

两处近乎同款的模板句：

| 位置 | 引用 | 判定 |
|:--|:--|:--|
| `skills_advanced_outline_p3.dart:72` | 「schema 已记录在审阅报告 **C51**」 | ✅ 正确——C51 条目明确「`[YS_OUTLINE]` schema 留存备查（已不输出），详见 A.8.4」（台账 L59/L298/L1600） |
| `skills_beginner_p6.dart:130` | 「schema 已记录在审阅报告 **C52**」 | ❌ **空引用**——C52 条目（台账 L262）是 gap-detector 幽灵链路删除，修复走 A.9.5 方案 A（删链路），**全台账无任何 GAP schema 留存记录**（grep "schema"×GAP/C52/留存 仅命中 C51 侧） |

**修法建议**：beginner_p6:130 改为「若将来要接结构化输出，先在审阅报告补 schema 记录再接线」或直接删句。1 行改动。
**附带（低优先级）**：`skills_advanced_outline_p4.dart:111`「不要同时复习超过 2 个到期症候——一次聚焦一个」行内上限 2 与聚焦 1 并存，可读作「≤2、宜 1」，建议明确措辞（与 C63/C66 的 B-22 家族一并裁决）。

### C68｜V-05 受控副本清单：6 处副本无登记（P2）

「改了 A 忘了 B」的结构性温床（本项目已实际发生：C55 漏改措辞 1 本体 = C65）。本轮裁决坐实 6 处：

| # | 副本对 | 内容 | 现状 |
|:--|:--|:--|:--|
| 1 | `skills_l1_core_p1.dart:106` ↔ `skills_l1_core_p3.dart:33` | 「不允许其他跳级或回退」 | **已知 N27**（V-05 家族登记在案） |
| 2 | `skills_beginner_p4.dart:81` ↔ `skills_l1_core_p2.dart:60` | R-009 红线「不要替用户构思——用户的世界观、角色、故事是用户的」逐字重复 | **新发现** |
| 3 | `skills_beginner_p4.dart:21` ↔ `skills_l1_core_p2.dart:28` | 「展示 0-200 字草稿」识别信号，同值 | **新发现** |
| 4 | `skills_beginner_p4.dart:58` ↔ `skills_l1_core_p2.dart:51` | 「每个问题只追问 1-2 轮」，同值 | **新发现** |
| 5 | `skills_advanced_outline_p4.dart:99` ↔ `skills_advanced_outline_p5.dart:124` | P2→P3 迁移条件「连续完成 ≥3 个训练任务」，同值 | **新发现** |
| 6 | `skills_beginner_p4.dart:161` ↔ `skills_diagnosis_p2.dart:235` | 「练习次数只能来自系统注入的训练记录，不要自己估算」，88% 近似 | **新发现** |

**修法建议**（二选一，倾向前者）：
- **A. 登记制**：在 `skill_registry.dart` 顶部注释或独立文档建立「受控副本登记表」（上表 6 行），并在 R-024/R-010 侧补一条「改副本对任一侧前先查登记表」——零 prompt 改动，零行为风险；
- **B. 收敛制**：逐对收敛为「L1 常驻侧权威 + 场景侧引用」——每处都改注入 prompt（高风险，须逐条过四道门禁+锚点）。

### 已知存量确认（不重复立项）

| 扫描候选 | 对应已知 |
|:--|:--|
| enum-doc ×2（l1_core_p2:111 取值缺 P3_TRAINING/P4_REVIEW） | **N21** |
| conflict-pair「suggested_phase」可选 vs 迁移动作 | **N38**（C53 并列根因） |
| conflict-pair「附加」必附加 vs 不附加 | **N29**（C53 首选根因，实验排除主因后仍待优先级补写） |
| conflict-pair「诊断块」 | **N30 家族** |
| threshold「展示/字」（100+ vs 200+） | **N34**（态度升档 100+ @ l1_core_p2:18 vs 相位跳级 200+ @ beginner_p3:51/114/124，语义近邻数字不同） |
| copy-drift #1（跳级回退） | **N27**（已计入 C68 清单 #1） |

---

## 三、有意分层（8 组，确认无冲突）

| 组 | 值分布 | 判定依据 |
|:--|:--|:--|
| 上限/个（问题/个、轮/个） | doubao 3 / yuesheng 2 / sensei 1（`skills_attitude.dart:30/83/130`） | 档位设计本体，`maxQuestionsPerRound` 显式标注 |
| 对话/轮 | 世界观 3 轮（beginner_p7:106）vs 大纲 4 轮（advanced_outline_p3:103） | 不同域的防拖沓上限，互不约束 |
| 文本/字 | ≥50（阶段1完成）/ ≥100（P0→P1 过渡）/ <200（信号）/ 4000（分片阈值）/ 500（诊断练习） | 各为独立闸门，语义不同 |
| 症候/% & 训练/% | 模板选择刻度 <60/≥60（p4:47/67）vs 全景分带 40-79/≥80（p4:158/71） | 两套刻度不同用途：模板 A/B 选择 vs 能力全景分层。50% 症候同时命中两者不矛盾；<40% 由教学状态兜底覆盖。**低风险备注**：两套边界数字并存（60 与 80），建议 p4:158 补一句与 :47/:67 的关系说明（可并入第 7 批 C59~C66 处理时顺手做） |
| 练习/轮 | 降维后再给 2 轮（training_p3:247）vs ≥3 轮触发重校准（training_p1:36） | 不同机制（降维补偿 vs 感知校准触发） |
| 训练/个 | P2→P3 ≥3（p4:99 / p5:124，两处**同值副本**）vs P3→P4 ≥6（p5:133） | 不同迁移门槛，数字本意分层；同值副本计入 C68 清单 #5 |
| 轮/轮 | 大纲阶段 1 / 1-2 轮，初学降层 ≥2 轮，训练达标连续 3 轮，大纲总上限 4 轮 | 对象各异（阶段流程 / 降层触发 / 达标判据 / 总上限） |
| 追问/轮 | 1-2 轮 ×2 | **同值副本**（计入 C68 清单 #4），数字无冲突 |

---

## 四、良性误聚（27 组）

主题词把不同对象的数字聚到同桶，数值差异均属语义不同，非冲突。代表性判定：

- 「原文/字」：100-300 字是训练模板占位符（training_p1:47）、200 字内≥3 次是 A016 技法触发（training_p4:242）、5-20 字是 evidence 引用长度（syndrome_kb:46/66）——三个不相干对象。
- 「描写/句」「对话/字」「段落/*」等组同理：few-shot 锚点数字（如 manual_2:32 的 300 字 L2 锚点、:136 的 6 句无意义对话）是**判定样例**不是**规则阈值**，与教学规则数字不同层。
- 「症候/轮」l1_core_p1:245 的「第 2 轮起」是轮次序号非数量，误报。
- enum-token ×7（P001/P002 旧号）：历史合并标注（manual_1:87「含原 P001 子类型」）、禁止示例、few-shot 溯源用，均为良性（A.12.21 已核）。

**扫描器改进项（下版）**：few-shot 锚点示例行（`> ` 引导或「锚点示例」语境）与规则行分离，预计可消掉约 15 组误聚。

---

## 五、修复批次建议（待确认，均未动代码）

| 批次 | 内容 | 风险 | 改动量 |
|:--|:--|:--|:--|
| **R1** | C67：beginner_p6:130 引用处理（删句或改写） | 低（该句是元信息 parenthetical，但仍在 prompt 内，须过四道门禁+锚点） | 1 行 |
| **R2** | N21：l1_core_p2:111 取值列表补 P3_TRAINING/P4_REVIEW | 低（文档性补充，锚点 9 case 漂移，须重生成快照） | 1 行 |
| **R3** | C68 方案 A：受控副本登记表（文档/注释层） | **零**（不碰 prompt） | 1 份登记表 |
| 维持 | N29 优先级补写、N30/N34/N38、C54/C56 决策 | — | 按既有第 7 批决策线 |

R1+R2 可合并为一个 commit（同为 prompt 单行修复，一次锚点重生成、一次四道门禁）。

---

## 六、证据索引

- 扫描输出：`docs/audits/_scan/skill_consistency_scan_2026-09-01.txt`（+ .json）
- 校准记分卡与工具：A.12.21、`scripts/skill_consistency_scan.py`
- C51/C52 定义：台账 L59（C51 schema 留存）、L262（C52 幽灵链路删除）、A.9.5/A.10
- P2→P3/P3→P4 迁移条件：`skills_advanced_outline_p5.dart:122-137`
- 达标率双刻度：`skills_advanced_outline_p4.dart:47/67/71/158`
