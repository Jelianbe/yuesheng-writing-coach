# 内容层修正 · 执行提示词包（2026-08-16）

> 用途：交给执行 AI 逐条执行。每个提示词自包含，不依赖本包之外的上下文。
> 配套依据：《内容层审查报告-2026-08-16.md》、《yuesheng-flutter 宪法 v0.2》。
> 分工：执行 AI 动手改代码；主对话 AI 做审核验收（四闸 + 逐条验收标准）。

---

## 〇、通用执行铁律（每项都适用，执行 AI 无需重复询问）

1. **真源**：只改 `D:\ai-teacher\yuesheng-flutter`（Flutter 版）；历史 `D:\teacher\yuesheng-android`（RN 版）只读参考、禁止改动。
2. **先读宪法**：`docs/yuesheng-flutter-宪法草案.md` §四（工程纪律）、§八（A1-A12 避坑）。
3. **四闸门禁**（每批完成，缺一不可）：`dart analyze` 0 issues + `dart format --set-exit-if-changed` 通过 + 受影响测试全绿 + 文档/注释同步（计数、标题、claims 与代码一致）。
4. **保守原则**：只改任务指定范围，禁止顺手重构；防御性代码（静默跳过/NO_OP/双重校验）来自真实事故，禁止简化。
5. **决策项**：标【需决策】的，先登记 D#（日期/选项/选择/理由/落点）再执行；未决策不得擅自选边。
6. **独立提交**：每项一个 commit，消息带编号（如 `fix(content): B1 接通三卡生产者`）。
7. **沙箱环境**：跑 `flutter/dart` 命令若"零输出 + exit 1"，是 `.bat` 被沙箱拦截，改调 `D:\flutter\bin\cache\dart-sdk\bin\dart.exe`（见 `.workbuddy/skills/flutter-sandbox-run/SKILL.md`）。

---

## 第一批：功能断点（用户可感知，优先）

### 【B1】接通三卡生产者（阶段总结/部分认同/诊断失败）

- **任务**：给三个"有 UI 无生产者"的消息卡补上写入调用点。
- **位置**：`lib/services/message_card_service.dart:518/543/560` 的 `insertPartialAgreementCard / insertPhaseSummaryCard / insertDiagnosisFailedCard`（当前全库零调用）；渲染分派 `message_card_dispatcher.dart:109-138`；chat_page 回调已接好。
- **现状**：三卡 insert 函数定义了但无任何调用，学员对话中永远收不到这三类卡。待办清单 L608/L633 已自认"预留未接"。
- **改法（推荐）**：在自然触发点插入写入——① FEEDBACK 子阶段结束时写 `phase_summary`；② 诊断解析/校验失败路径写 `diagnosis_failed`；③ 学员对诊断"部分认同"路径写 `partial_agreement`。落点语义对齐 RN 版 `message-card-service.ts` 原实现。
- **约束**：写入失败不阻断主流程（降级 `try/catch` + 接 error_logger）；卡片消息要进 messages 表并携带正确 `message_type`。
- **验收**：grep 证明三个 insert 函数各有 ≥1 个非测试调用点；补一个单测（FEEDBACK 结束产出 phase_summary 卡）；四闸全绿。

### 【B2】同侪感话术整改【需决策】

- **任务**：消除"AI 编造群体数据"的指令，改为有真实数据源的对比。
- **位置**：`lib/services/skill_registry.dart:2622-2642`（feedback-cognition §7.5）、`:1707-1718`（coaching-rhythm §5.5）——现指示 AI 输出"这个进度比很多学员都快""大多数人需要练 3-5 次"等群体对比。
- **现状**：单机单学员架构下无任何群体统计代码，LLM 只能编造。
- **改法（推荐主线）**：改为"与学员自身历史对比"——基于已有的 `teaching_history` / 评估数据（`evaluation_service.dart`、`student_profile_compute.dart`），让 AI 说"你这次比上次 X 有进步"而非"比大多数学员快"。备选（不推荐）：直接删除群体对比话术。
- **验收**：grep `同侪|群体|同伴|大多数学员` 确认无群体对比指令残留；新指令引用学员自身历史数据；四闸全绿。

### 【B3】技法熟练度派生（闭合 P3/P4 gating）

- **任务**：给 advanced-phases 的"技法熟练度"门槛提供真实可计算的数据源。
- **位置**：`lib/services/skill_registry.dart:5248/5268-5272/5303/5358-5359/5475`（用"技法熟练度 <60% / ≥60% / ≥80%"做模板选择、能力地图、P2→P3 迁移条件）；现状只有整体 `inferProficiency`（`student_profile_compute.dart:133`）和症候级 FSM，无技法级（T001-T031）数据。
- **改法（推荐）**：技法熟练度 = 由该技法覆盖的症候熟练度派生（`kTechniquesBySyndrome` 映射 + 各症候 passRate/severity 趋势加权），新增一个纯函数 `inferTechniqueProficiency`，注入到 advanced-phases gating 上下文。
- **约束**：纯函数、无 IO、可单测；不改变现有症候级 FSM 语义。
- **验收**：advanced-phases 的 gating 条件可被真实计算；补单测覆盖派生函数；四闸全绿。

### 【B4】接通态度升降档代码层

- **任务**：让文档宣称的"负反馈降档 / 正反馈升档 / 安全词降档"真正由代码执行，而非只靠 LLM 自觉。
- **位置**：`lib/config/shared_constants.dart:266/278`（`containsNegativeFeedback` / `isSafetyWordRequest` 当前零调用）；`lib/widgets/chat_attitude.dart:59-65`（`suggestAttitudeAdjustment` 唯一调用点，不传正/负反馈计数，恒 0）；`lib/services/attitude_advisor.dart:74-77`（升降档分支因此永不触发）。
- **改法（推荐）**：在用户消息发送路径接入检测——① 发送前用 `isSafetyWordRequest` 检测「轻一点」→ 无条件降一档；② 扫描用户消息，命中负反馈信号累加计数、达 `negativeFeedbackDowngradeCount`(3) 降档；③ 正反馈信号累加达 `positiveFeedbackUpgradeCount`(4) 升档；把计数传入 `suggestAttitudeAdjustment`。
- **约束**：安全词降档优先于一切；检测函数保持纯函数；不改变态度三档的 skill 内容。
- **验收**：`isSafetyWordRequest` 有 ≥1 调用点；计数不再恒 0；补测试（「轻一点」触发降档、3 次负反馈降档）；四闸全绿。

---

## 第二批：展示层对齐（防 AI 拿矛盾/泛化指令）

### 【B5】training-templates 补齐 P021-P041 定制块

- **任务**：把训练模板定制块从 18 症候扩到 39。
- **位置**：`lib/services/skill_registry.dart:4771-4790/4827-4844/4854-4879`（块 A/块 C 仅覆盖 P003-P020；标题自注"18 症候 × 3 块" :4742；P021-P041 走兜底通用框架）。
- **改法**：按 P003-P020 同构，为 P021-P041 共 21 个症候补块 A（检验/标注/清单起点）与块 C（进阶变体）；内容做"锚点"不做大段话术（遵守宪法 §一设计原则）。
- **约束**：新增块必须经 `four_libraries_consistency_test` 校验；更新"18 症候"标题与覆盖注释。
- **验收**：39 症候均有非兜底定制块；一致性测试全绿；四闸全绿。

### 【B6】动作正文「适用症候」与注册表对齐

- **任务**：消除动作正文与注册表 actions 的矛盾（16 个中 13 个不一致）。
- **位置**：`lib/services/skill_registry.dart:2667-3025`（动作正文）vs `lib/services/syndrome_registry.dart:144-761`（注册表 actions 真源）。
- **改法（推荐）**：动作正文「适用症候」改为从注册表派生（现有 v1/v2 映射表已派生），删除手写硬编码；若无法派生则逐条对齐。
- **验收**：正文与注册表无矛盾；grep 无旧表述"含原 P001 子类型"残留（改用 v1ActionName/v2ActionName）；四闸全绿。

### 【B7】孤儿技法 T028/T031 处置【需决策】

- **任务**：解决 T028 反转设计法、T031 氛围构建法"索引标注适用、注册表零引用"的矛盾。
- **位置**：`lib/services/technique_knowledge_base.dart:742/811`（技法正文）vs `syndrome_registry.dart`（39 症候 techniques 零命中 T028/T031）。
- **改法（推荐主线）**：接入映射——T028 反转设计法接入 P014/P015/P024/P027（结尾/高潮/期待感/追读）；T031 氛围构建法接入 P003/P008/P028/P029（画面/氛围/场景）。备选：从 L2/L3 索引撤掉这两个技法的"适用"标注。
- **验收**：T028/T031 在注册表 ≥1 症候引用；索引与注册表一致；四闸全绿。

### 【B8】重叠优先级表补 4 行

- **任务**：补齐缺失的症候间排序优先级依据。
- **位置**：`lib/services/syndrome_knowledge_base.dart:147-188`（重叠优先级表 42 行，缺 P022/P023/P024/P026）。
- **改法**：补 P023↔P015/P012、P024↔P017、P026↔P024、P022↔P007 的优先级行，按"哪个更具体/更上游"给出优先判定。
- **验收**：表中新增 4 组症候的优先级；多症候命中排序有表级依据；四闸全绿。

### 【B9】技法静态映射表补 P032-P041（或改派生）

- **任务**：消除技法库内嵌静态映射表与注册表派生表的不一致。
- **位置**：`lib/services/technique_knowledge_base.dart:832-864`（静态表止于 P031，缺 P032-P041 共 10 症候）。
- **改法（推荐）**：直接改用已存在的 `kTechniquesBySyndromeDerived`（`syndrome_registry.dart:799-802`）作为唯一检索源，删除静态表；若保留静态表则补齐 10 症候。
- **验收**：L3 检索源覆盖 39 症候；两套表不再并存矛盾；四闸全绿。

---

## 第三批：文档/预算/双轨清理

### 【B10】token 预算表重测 + 降级顺序修正

- **任务**：让预算表反映当前内容体量与真实裁剪顺序。
- **位置**：`lib/config/token_budget_table.dart:9-10/96`（预算值陈旧：L1 写 10k 实测 ≈14.4k；L2 diagnosis 写 16.5k 实测 ≈21.5k；降级顺序首位"L2 组数"实际不可裁）。
- **改法**：按项目自身口径（char×0.4，`skill_dispatcher.dart:185`）重测 L1/L2/L3，更新数值；修正降级顺序说明（L2 内嵌 systemPrompt 不可裁，真实先裁 L3 教学内容/检测器/训练知识）。
- **验收**：预算值与实测一致；降级顺序与代码真实行为一致；四闸全绿。

### 【B11】文档/注释计数刷新（29→39）

- **任务**：消除过时计数声明。
- **位置**：`docs/教学诊断训练资产清单.md:217`（"29 症候全量"）、`lib/services/training_knowledge_base.dart:13-15/20/1064`、`lib/services/syndrome_knowledge_base.dart:32/114/122`（"P003-P031""14100 tokens"）、`docs/教学机制审查包.md:191-193`（过时互斥/反例描述）。
- **改法**：全部更新到 39 症候（P003-P041）与当前行数/体积。
- **验收**：grep 无"29 症候""P003-P031 全部"等陈旧声明残留。

### 【B12】v1/v2 双轨技能定去留【需决策】

- **任务**：消除 v1/v2 双轨技能并存的维护漂移风险。
- **位置**：`lib/services/skill_registry.dart:5920-5928`（pilot=true 下 training-loop/templates/evaluation/text-surgery/coaching-actions 五个 v1 永不加载成死负载）；两套轮次上限并存（training-loop ≤3 :4228 vs v2 分组 3/5/5 :4330-4336）。
- **改法（推荐主线）**：确认 v2 覆盖全部 v1 语义后，**转正 v2、删除 v1 死负载**；若暂不删，则在注册表显式标注 v1 为 `@deprecated fallback` 并统一轮次口径为 v2 分组。
- **验收**：单一活跃技能集（或显式标注回退）；无"两套口径 AI 随机加载"风险；四闸全绿。

---

## 数据/UI/安全层修正（B13-B25）

### 【B13】outline_impression 'expired' CHECK 三方对齐【需决策】（数据层致命）
- 任务：修复大纲印象 status='expired' 的三方脱节（与 RN 版 file CHECK 同型复发）。
- 位置：`lib/data/repositories/outline_repository.dart:230/249`（写 'expired'）、`lib/data/database/database.dart:530-531`（v18 CHECK 只允许 pending/active/rejected/superseded）、`lib/data/database/tables.dart:656-658`（drift 定义无 CHECK）。
- 现状：新装库（无 CHECK）写入成功；升级库（v18 带 CHECK）写 'expired' 必抛 CHECK 失败，被 `chat_service_diagnosis.dart:597-599` 吞掉 → 升级用户大纲记忆静默报废。
- 改法（推荐主线）：三方统一——① drift 表定义补 CHECK（含 'expired'，或确定过期语义改用 'superseded'）；② 若需扩 CHECK 加新迁移；③ **迁移 SQL 与 drift 表定义由单一真源生成，杜绝双 schema 分叉**。
- 验收：SQLite 实测（新装 + v18 升级两条路径）写 'expired' 均成功；grep 无 CHECK 与写入值不一致；补回归测试。

### 【B14】跨会话流式状态泄漏（UI 致命）
- 位置：`lib/providers/chat_store.dart:79-172`（isStreaming/streamingContent 不带 sessionId）、`lib/widgets/chat_teaching.dart:282-290`（onComplete 闭包捕获旧 sessionId）、`lib/widgets/chat_session.dart:30-35`（切会话不清流式态）。
- 改法：流式状态加 sessionId 维度；切/建会话清空流式状态；onComplete 校验当前会话==发送时会话，不一致丢弃。
- 验收：切会话后旧流式气泡不再渲染到新会话；补测试；四闸全绿。

### 【B15】deleteOrphanSessions 补 student_model 清理
- 位置：`lib/data/repositories/session_repository.dart:398-420`（漏删 student_model）；student_model.session_id NOT NULL + FK SET NULL（tables.dart:286-287）。
- 改法：删会话前先删关联 student_model 行（对齐 deleteSession:374-388 既有做法）。
- 验收："清除缓存"不再报错；补测试。

### 【B16】v24 重建 chapters 保留 chapter_id 缓存
- 位置：`lib/data/database/database.dart:667`（DROP TABLE chapters 隐式清空 sessions.chapter_id）。
- 改法：重建后回填 sessions.chapter_id（从 session_reference 或 chapters 关联恢复）；或迁移中先备份再回填。
- 验收：v23→v24 升级后会话 chapter_id 不丢、不重复建会话；补迁移测试。

### 【B17】editor_observation UPSERT 冲突目标修正
- 位置：`lib/data/repositories/editor_observation_repository.dart:54`（insertOnConflictUpdate 冲突目标为主键 uuid，UNIQUE(session_id,message_id) 不吸收）。
- 改法：改原生 SQL `ON CONFLICT(session_id, message_id) DO UPDATE`（对齐 reference_repository.dart:283-289 的既有正确做法）。
- 验收：重复 (session,message) 不再抛 UNIQUE violation；补测试。

### 【B18】流式滚动劫持 + 每 token rebuild
- 位置：`lib/widgets/message_list.dart:260-283`（didUpdateWidget 无条件滚底）、`chat_store.dart:120-122`（每 token 新字符串+新 state）、`chat_page.dart:163-164`（整页 watch rebuild）。
- 改法：① 仅用户已在底部时才自动滚；② 流式内容增量拼接（TextSpan/局部状态），避免整页 rebuild 与 O(n²)；至少节流 100ms。
- 验收：上翻不被拽回；流式无明显卡顿；补性能基线/测试。

### 【B19】消息加载竞态加会话守卫
- 位置：`lib/widgets/chat_page.dart:208-211`（fire-and-forget listMessages then setMessages）。
- 改法：查询带 sessionId，完成后校验当前会话==查询会话再 setMessages；或加序号/取消旧请求。
- 验收：快速连切会话不串消息；补测试。

### 【B20】作品详情/设置页三态补全
- 位置：`manuscript_detail_page.dart:288-310`（章节 Tab 无错误态）、`file_section.dart:51-55,167-215`（文件 Tab 无 loading/error）、`settings_page.dart:87-89,110-123`（静默失败，配置读失败误显"尚未配置"）。
- 改法：章节 Tab 消费 error；文件 Tab 加 try/catch + 三态；设置页失败给提示且不误报未配置。
- 验收：三态齐全、失败有反馈；补测试。

### 【B21】Manifest 权限 maxSdkVersion【需决策】
- 位置：`android/app/src/main/AndroidManifest.xml:10-13`（READ/WRITE_EXTERNAL_STORAGE）。
- 改法（推荐）：加 `android:maxSdkVersion="32"`（仅 Android<13 需要）；备选：确认 file_picker 10.x SAF 后移除。与宪法 §五.4 对齐。
- 验收：Manifest 与宪法不冲突。

### 【B22】错误体 Authorization 脱敏
- 位置：`lib/services/llm_client.dart:110-121`（_buildDioError 截断 100 字符但未脱敏 Authorization 头回显）。
- 改法：展示前正则脱敏 `Bearer <key>` / `Authorization:` 头。
- 验收：错误信息不含密钥。

### 【B23】多步写事务化统一
- 位置：`session_repository.dart:21-44,50-112,117-188`（建会话三连写）、`diagnosis_repository.dart:396-435,490-511`（resolve/remove/confirm 非原子）。
- 改法：统一 transaction 包裹多步写（对齐 commitDiagnosis:125）。
- 验收：任一步失败不留半成品。

### 【B24】UI 一般项收敛（定时器/静默 catch/暗色硬编码）
- 位置：`chat_attitude.dart:89-91`（500ms 定时器未取消）、7 处无日志 catch、`writing_page.dart:552` 等暗夜色值硬编码。
- 改法：定时器持有引用并 dispose 取消；静默 catch 补 debugPrint/error_logger；暗夜色值收敛进 AppColors。
- 验收：无 dispose 后 ref 崩溃；无静默 catch；色值走 token。

### 【B25】UI 一般项（非懒加载/重复组件/孤儿 KV）
- 位置：`growth_content.dart:247-265` 等非懒加载列表、色条卡片/删除弹窗/格式化函数重复、`chapter_repository.dart:158-168` 孤儿 KV。
- 改法：for 循环改 ListView.builder；抽公共组件；章节删除清理 chapter_draft/chapter_versions KV。
- 验收：长列表懒加载、无重复实现、无孤儿 KV。

---

## 性能/测试质量层修正（B26-B30）

### 【B26】token 估算口径修正（char×0.4 → 中文口径）
- 位置：`lib/config/shared_constants.dart:99`（charToTokenRatio=0.4）、`lib/services/token_budget_guard.dart:66-67`、`lib/services/skill_dispatcher.dart:183-185`（三处统一 char×0.4）。
- 现状：0.4 是英文口径，中文实际 0.6~1.0 token/字符，系统性低估 2~2.5 倍 → 预算闸门（total<=50000）几乎永不触发，实际超预算不自知；这正是 L1"实测 14.4k vs 预算 10k"漂移的根因。
- 改法：改中文口径（≈1.0 token/char）或引入真实 tokenizer；用新口径重测预算表（与 B10 合并执行）。
- 验收：预算闸门能真实触发；L1/L2/L3 预算值与新口径实测一致。

### 【B27】书架 N+1 查询优化
- 位置：`lib/providers/manuscript_providers.dart:232-242`（每卡 watch stats）、`lib/data/repositories/chapter_repository.dart:108-117`（listChapters 返回含 content 全列）、`bookshelf_page.dart:115-122`（刷新循环 invalidate N 次）。
- 改法：一条 `SELECT COUNT(*), SUM(word_count) ... WHERE manuscript_id=? AND status!='archived'` 聚合替换；刷新改单次批量重查。
- 验收：书架加载查询次数降为 O(1)；不再拉章节正文进内存。

### 【B28】其他性能项收敛
- 位置：`chapter_tree_drawer.dart:165-207`（章节树非懒加载）、`main.dart:168-181,259-272`（冷启动首帧空白页，DB 串行门控）、`session_repository.dart:245-253`（listRelatedSessions 全表扫）、`diagnosis_repository.dart:344-385`（全表 SELECT+内存 group）、`manuscript_providers.dart:264-266`（大纲 N+1）。
- 改法：章节树改 ListView.builder；冷启动先渲染占位壳再异步 DB；全表扫改 SQL 下推（WHERE/GROUP BY）；大纲 N+1 合并查询。
- 验收：长列表懒加载、冷启动非空白、查询下推。

### 【B29】LLM 网络层契约测试（真实 HTTP）
- 位置：`lib/services/llm_client.dart`（真实 Dio，SSE/chatCompletion/testLlmConnection/超时/错误映射），现 29 个测试文件全用 FakeLlmClient 继承覆盖，真实 HTTP 从未执行。
- 改法：用 Dio mock adapter（非继承 fake）给 llm_client 加契约测试：SSE 分帧、`[DONE]` 解析、dio 异常→错误映射、CancelToken、超时、API key 认证头。
- 验收：llm_client 真实逻辑有测试；SSE/错误映射/超时路径被断言。

### 【B30】迁移逐版本测试 + 流式状态回归断言 + 清理死测试
- 位置：`test/data/database/`（现只有 migration_v23/v24 两个）、`chat_store_test.dart:136-143`（setSessionId 弱断言）、`dao_repository_test.dart:453-477`（零断言死测试）、`widget_test.dart:12-15`（占位断言）。
- 改法：① v5-v25 每段迁移补"手造存量库→onUpgrade→断言 CHECK/FK/数据保留"测试（重点 v18 outline 'expired'）；② setSessionId 测试补"切换即重置流式状态"回归断言（拦跨会话泄漏）；③ 删除零断言死测试与占位断言。
- 验收：迁移逐版本覆盖；流式泄漏有回归测试；无死测试虚增覆盖。

---

## 审核验收协议（主对话 AI = 审核主力）

执行 AI 每完成一批，主对话 AI 做以下验收：
1. **四闸复核**：独立跑 `dart analyze`（0）+ 受影响测试（全绿）+ `dart format`（通过）。
2. **逐条验收标准**：按每个提示词的"验收"清单逐项 grep/读码核实，不信自述。
3. **无回归**：对照 `four_libraries_consistency_test` 与既有测试，确认没有因改动引入新漂移。
4. **决策闭环**：含【需决策】项，确认 D# 已登记、选择与执行一致。
5. **结论**：出「验收报告」，逐项标记 ✅/❌/需返工，附证据（命令输出/行号）。
