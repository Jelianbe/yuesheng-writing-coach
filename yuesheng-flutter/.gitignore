# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/build/

# 调试/临时产物（本地开发资产，不入库）
tmp_*
screenshots_*.png
screen_*.png
/lib/main_dev.dart
/lib/preview/

# Prompt 行为验证导出的 system prompt 全文（每份约 7 万字符，可随时重导）
# 由 buildSystemPromptV2 导出，属一次性验证产物，不入库。
# 目录结构：after_b2/ ~ after_b4/ 依次为第 2、3、4 批修复后的基线，
# 相邻两批构成前后对照双盲实测的材料（见审阅报告 A.8 / A.10）。
# 第 6 批（C53 行为复测 + C54 ADR + 剩余 B 组条目）回归对比仍需使用，届时再清理。
/docs/audits/_prompt_dump/

# 沙箱隔离用的 Gradle 缓存（非项目依赖，禁止入库）
.gradle_home/

# 门禁产物（scripts/gate.sh 输出）
outputs/

# 门禁产物（scripts/gate.sh 输出）
outputs/
