# 月笙写作教练（Yuesheng Writing Coach）

> AI 驱动的中文小说写作辅导工具 · **写作教练，不是写作助手**

月笙写作教练面向中文小说写作者，**不替你写句子，也不替你做创作决策**——而是通过「诊断 → 教学 → 训练 → 复盘」的闭环，帮你识别写作中的系统性问题，并提供针对性训练。

**定位**：不生成内容，只提升能力。

---

## 普通用户 · 只想装到手机上用

本应用是 **Android 手机应用**。用起来**不需要 GitHub 账号**，也不需要看懂下面任何技术内容。

**当前状态：尚无可下载的安装包。**

源码已经公开，但**「能拿到源码」不等于「能用上」**——我们还没建立面向普通用户的发布通道。
（在 GitHub Releases 里看到的版本属于**早期原型**，与现在的手机应用不是同一个东西。）

发布通道打通后，下载入口会直接写在这一节。在那之前：

| 你是 | 怎么做 |
|:--|:--|
| 只想用 | 关注本仓库的 Releases，等待发布 |
| 有 Flutter 开发环境 | 见下方「开发者 · 快速开始」，自行构建 |

**两点须知**

- **自带密钥（BYOK）**：需自备一家大模型服务的 API Key。应用**不内置密钥**，也**不经任何中转服务器**。
- **稿件不出手机**：小说草稿、诊断结果只写入本机数据库，不上传。

> 你不需要「相信」上面两条 —— 这正是源码公开的原因：
> 手机应用是个黑盒，源码是唯一能让你（或任何人）自己验证「怎么说就怎么做」的通道。

---

## 仓库结构

| 目录 | 状态 | 说明 |
|:--|:--:|:--|
| **`yuesheng-flutter/`** | ✅ 活跃真源 | Flutter + Dart 实现，所有开发在这里进行 |
| `yuesheng-writing-coach/` | 归档 | 早期 Electron + React 原型，已停止开发，仅作历史参考 |

---

## 核心能力

- **五阶段教学状态机**（P0 初始 → P1 教学 → P2 训练 → P3 评估 → P4 复盘）驱动对话流程
- **诊断引擎**：分析写作片段，识别症候并排序（当前注册表 41 条，P001–P041）
- **五步训练流**：解说 → 例证 → 确认 → 尝试 → 反馈，配套写作技法库按症候动态填充
- **能力图谱与训练推荐**：基于诊断结果推荐针对性训练，支撑进度追踪
- **学生模型**：基于本地数据库（drift/SQLite）的学习画像，记录诊断历史与训练进度
- **症候跨轮次历史引用**：诊断卡可回溯历史表现，标注好转或复发的进展

---

## 技术栈（yuesheng-flutter）

| 层 | 技术 |
|:---|:-----|
| 框架 | Flutter + Dart |
| 状态管理 | Riverpod |
| 持久化 | drift（SQLite） |
| 样式 | 设计令牌体系（竹青 `#2D5A52` 色系） |
| LLM 接入 | 多供应商适配（OpenAI 兼容 / DeepSeek / Kimi / 智谱 / 豆包等），支持自定义 baseUrl 与多账号 |
| 测试 | Flutter test（**用例数 / 文件数不复述**，以 `bash scripts/gate.sh` 实测为准） |

## 质量门禁

开发流程内置一整套质量门禁，`bash scripts/gate.sh` 一键全跑。

> **本文不复述门禁道数与用例数。** 按 `CONTRIBUTING.md` 的约定
> （「不要在本仓任何文档里复述会漂移的数字」），一切以脚本实测为准 ——
> 本节原写「六道门禁 / 2600+ 用例」，两个数都已漂移。

门禁覆盖的方面（**增删以 `scripts/gate.sh` 自身为准**）：

- 代码格式（`dart format`）
- 静态分析（`flutter analyze`）
- 全量测试（`flutter test`）
- 循环依赖检查
- 安全 / 密钥扫描
- R-019 函数行数硬上限
- 其余：覆盖率、prompt 反模式、内容元数据、交互回归、伪拆分检查、A 类豁免

---

## 开发者 · 快速开始

### 前置依赖

- Flutter SDK（稳定版）
- Android SDK / Xcode（移动端构建）

### 运行

```bash
cd yuesheng-flutter
flutter pub get
flutter run
```

### 测试

```bash
cd yuesheng-flutter
flutter test
```

---

## 发布状态

> **本节原写「Git tag 最新 `v5.0.0` / GitHub Release `v1.4.0`」—— 字面为真，但严重误导。**
> 实测（2026-09-19）：`v5.0.0` 只有 tag，**无对应 Release、无产物**；
> `v1.4.0` 是**已归档的 Electron 原型**，其说明里的运行方式还是 `npm install`。

| 标识 | 实际情况 |
|:--|:--|
| 应用版本号 | 真源 = `yuesheng-flutter/pubspec.yaml` 的 `version`（本文不复述，见 `CHANGELOG.md`） |
| Git tag | 存在历史里程碑 tag，**早期原型与当前应用混用编号** ⇒ tag 号**不能**当作「可下载版本号」 |
| GitHub Release | ⚠️ 现存 Release 全部属于**已归档的原型**，**不含当前应用的安装包** |
| 安装包 | ❌ **当前应用尚未发布任何版本**（`CHANGELOG` 至今只有 `[未发布]` 一节） |
| 发布形态 | **安装包**（非库）—— 见 `CHANGELOG.md` |
| 分发方式 | 发布时通过 Release 附件提供（本仓库**不存放安装包**，构建产物目录在 `.gitignore` 内） |

---

## 反馈

欢迎通过 GitHub Issues 提交 bug 或功能建议。
