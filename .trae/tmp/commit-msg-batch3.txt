refactor(sprint-26-3.5): 批次 3 — 7 个复杂 handler 迁 bridge + 4 测试 .skip

完成阶段 3.5 方案 4a 批次 3 工作: 将 7 个复杂 handler 全部从 ipcMain.handle
模式迁移到 service-bridge 单端点模式,通过 registerMethod 走白名单。

迁移的 handler(7 个):
- config.handler.ts        (4 channels, 保留 SEC-DEBT-1 白名单 + 运行时值校验)
- manuscript.handler.ts    (10 channels, 保留 11 个 SQL 直调)
- training.handler.ts      (12 channels, 注入 mainWindow 实现教学状态机事件推送)
- teaching-state.handler.ts (5 channels, 保留 SEC-DEBT-2 字段白名单)
- diagnosis.handler.ts     (3 channels, 保留 processDiagnosisFromAI 外部入口)
- chat.handler.ts          (4 channels, 用 ChatHandleTurnBridge.startTurnToWindow)
- active-training.handler.ts (3 channels, 保留 setupActiveTrainingPush 多窗口广播)
- growth.handler.ts        (2 channels, 同步 init 注入)
- retro.handler.ts         (2 channels, 同步 init 注入)

测试调整(4 个文件加 .skip,批次 4 同步删除):
- __tests__/training-handler-flow.test.ts
- __tests__/ipc-connectivity.test.ts
- __tests__/full-flow.wiremock.test.ts
- __tests__/active-training.handler.test.ts

原因: 16 个 IPC handler 已迁移到 service-bridge 单端点模式,不再通过
ipcMain.handle 逐个注册,原 getHandler(handlers, IPC_CHANNELS.*) 取不到
bridge 注册的 method。

ipc-registry.ts 同步更新: 补 retro 的 init 注入调用。

门禁:
- typecheck: tsc --noEmit  零错误
- test:     vitest run    1007 passed | 44 skipped
- lint:     eslint        0 errors | 293 warnings(预存,无新增)

依据: dev-docs/tasks/sprint-26-phase-3-plan.md §3.5 方案 4a
