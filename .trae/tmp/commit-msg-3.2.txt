refactor(sprint-26-3.2): active-training.service 双轨化 + isCapacitor 改用 userAgent

active-training.service.ts:
- 3 个 invoke 方法 (updateDraft/get/submitStep) 走 runDualTrack
- subscribe 保持 IPC-only (Capacitor 端无事件推送,降级为 noop + warn)
- Android 端直接 import shared/services/active-training.service.ts

_dual-track.ts:
- isCapacitor() 改用 navigator.userAgent 检测(原 !!window.Capacitor 在 jsdom 误判)
- 注释说明 @capacitor/core 在 jsdom 自动注入 Capacitor 对象,需用 userAgent 区分

_dual-track.test.ts:
- 8 个测试改用 navigator.userAgent mock 替代 window.Capacitor mock

test/setup.ts:
- 注释说明 Capacitor 注入策略

storage-adapter.poc.test.ts:
- 修正表名 'training_records' → 'user_training_records' (pre-existing bug,跟 3.2 一起 ship)

依据: dev-docs/tasks/sprint-26-phase-3-plan.md §3.2 / D-074
门禁: typecheck 0 error / vitest 1051/1051 pass
