# FB20260617-011 RWR-P0-6 useRightPanel hook + 设置面板

> 任务类型：A 类 / P0 / 中型 🎯
> 触发：RWR-P0-6 任务规格（useRightPanel hook + 设置面板）
> 前置：RWR-P0-3（useRightPanelStore 8 action）
> 后置：P1-3 / P3-1（依赖 useRightPanel + SettingsPanel）

## 1. 任务概述

按 RWR-P0-6 规格创建 useRightPanel hook 封装层 + 重写 SettingsPanel 符合规格 DoD。规格要求提供 open/close/toggle/setTool/setView 5 方法，设置面板包含 API Key 配置 + 态度档位默认偏好。

## 2. 关键发现：SettingsPanel 已存在

预分析发现 `src/renderer/components/settings/SettingsPanel.tsx` 已存在（325 行，V2-022 时期），包含 API Key / Base URL / 模型 / Temperature / 态度模式（2 态）/ 连接测试 / 主题切换等 8 项功能。

按 R-007 双向绑定 + R-009 用户主权 + R-021 最小化，提供 3 个选项让用户决策：
- A) 跳过重写（推荐，规格 DoD 已满足）
- B) 增量添加 attitude 第 3 态
- C) 重写 SettingsPanel

**用户决策：C) 重写 SettingsPanel**

理由：R-007 规格要求严格按规格执行，不保留规格外的功能。

## 3. 重写差异

| 项目 | V2-022 版 | RWR-P0-6 重写版 | 备注 |
|------|----------|----------------|------|
| 温度滑块 | ✅ 有 | ❌ 删除 | 规格未要求 |
| 连接测试 | ✅ 有 | ❌ 删除 | 规格未要求 |
| 主题切换 | ✅ 有 | ❌ 删除 | 规格未要求 |
| 态度模式 | 2 态（温柔/严格）| **3 态**（温柔/月笙/尖锐）| 补齐类型定义 |
| 内联样式 | ✅ | ❌ → CSS Module | 符合项目规范 |
| useRightPanel hook | ❌ 无 | ✅ 集成 | 规格 DoD |

## 4. 关键设计

### 4.1 useRightPanel hook（77 行）

```ts
export const useRightPanel = (): UseRightPanelReturn => {
  // X-01 协议: isOpen/activeViewId 从 drawer.store 派生(只读)
  const isOpen = useDrawerStore((s) => !s.collapsed);
  const activeViewId = useDrawerStore((s) => s.activePanel);

  // 稳定引用(zustand action 函数引用稳定)
  const openTool = useRightPanelStore((s) => s.openTool);
  const close = useRightPanelStore((s) => s.close);
  const switchTo = useRightPanelStore((s) => s.switchTo);

  return {
    isOpen, activeViewId,
    open: () => openTool(DEFAULT_OPEN_TOOL),  // 默认 search
    close, setTool: openTool, setView: switchTo,
    // toggle() 暂不实现, 统一由 panel-session 管理
  };
};
```

**X-01 协议合规性**：
- ✅ 不创建新状态（仅派生只读字段）
- ✅ 复用 RWR-P0-3 useRightPanelStore 8 action
- ✅ 组件层不直接协调 3 个 store

### 4.2 SettingsPanel 状态机

| 状态 | 触发 | 转换 |
|------|------|------|
| `idle` | 初始 / 保存 2s 后 | → saving（点保存）|
| `saving` | 点击保存按钮 | → success（IPC 成功）|
| `saving` | 点击保存按钮 | → error（IPC 失败）|
| `success` | 自动 2s 后 | → idle |
| `error` | 手动点保存 | → saving |

### 4.3 attitude 3 态 UI 映射

| 内部值 | UI 标签 | 描述 |
|--------|---------|------|
| `doubao` | 温柔 | 多鼓励，少批评 |
| `yuesheng` | 月笙 | 平衡，有建议有肯定 |
| `direct` | 尖锐 | 直接指出问题 |

## 5. R-030 10 步执行记录

| 步骤 | 操作 | 结果 |
|------|------|------|
| Step -1 | 预分类 A 类 / P0 / 中型 | ✅ |
| Step 0 | 查重（无重复任务） | ✅ |
| Step 1 | 溯源 + 发现 SettingsPanel 已存在 + 用户决策重写 | ✅ |
| Step 2 | 拆分 F-001~F-004（hook + UI + CSS + 验收）| ✅ |
| Step 3 | 用户确认 3 个决策（attitude 3 态 / 重写策略 / hook 派生）| ✅ |
| Step 3.5 | tag v1.0.0-pre-FB20260617-011 | ✅ |
| Step 4 | F-001 hook (77行) + F-002 UI (253行) + F-003 CSS (170行) | ✅ |
| Step 4.5 | tsc 4 errors → 修复 → 0 errors + vitest 174 passed | ✅ |
| Step 5 | DoD 5 项全过 | ✅ |
| Step 5.5 | commit + tag v1.0.0-post | ✅ |
| Step 6 | 报告归档 + TASK-CHAIN V17.4 | ✅ |

## 6. 关键决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| SettingsPanel 策略 | 重写 | 用户决策(R-007 严格按规格) |
| attitude 文案 | 温柔/月笙/尖锐 | 内部值 3 态，UI 显示中文 |
| isOpen/activeViewId 来源 | drawer.store 派生 | X-01 协议(SSOT: drawer) |
| toggle() 实现 | 暂不实现 + 注释 | 规格说"由 panel-session 管理" |
| CSS Module 路径 | `src/renderer/styles/` | 符合项目结构（layout/ components/ stores/）|

## 7. 双向绑定状态同步

| 状态源 | 内容 | 状态 |
|--------|------|------|
| useRightPanelStore | 8 action 无修改 | ✅ 完整 |
| useDrawerStore | 派生 isOpen/activeViewId | ✅ 只读 |
| useConfigStore | 9 action + 14 字段 | ✅ 全部接入 |
| SettingsPanel | 重写 253 行 | ✅ 规格 DoD |
| CSS Module | 170 行 | ✅ 完整 |
| tsc | 0 errors | ✅ |
| vitest | 174 passed | ✅ |

## 8. 经验教训

### 8.1 决策点必须事前暴露
- 预分析发现 SettingsPanel 已存在（V2-022 时期）
- 在执行前 3 个选项让用户决策（跳过 / 增量 / 重写）
- **避免**：直接重写 → 丢失 325 行已测试功能 → 用户惊讶
- **改进**：R-022 过程可见 + 决策点前移

### 8.2 X-01 协议派生字段
- useRightPanelStore 不存 isOpen/activeViewId（按 X-01 协议从 drawer.store 派生）
- 修复时一次 tsc 错误暴露了"我对 store 字段的猜测"
- **改进**：先 Read 现有 store 真实签名，再写 hook

### 8.3 严格按规格 vs 最小化
- R-007 双向绑定（严格按规格）+ R-021 最小化（不重复造轮子）冲突
- 用户决策时选择严格按规格（重写 325 行已存在 UI）
- **教训**：R-009 用户主权 > R-007 vs R-021 矛盾

## 9. 关联文档

- [RWR-P0-3 报告](file:///d:/ai-teacher/yuesheng-writing-coach/docs/reports/FB20260617-003-报告.md) — useRightPanelStore
- [TASK-CHAIN.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/TASK-CHAIN.md) — 任务链（V17.4 推进）
- [SettingsPanel.tsx](file:///d:/ai-teacher/yuesheng-writing-coach/src/renderer/components/settings/SettingsPanel.tsx) — 重写版
- [useRightPanel.ts](file:///d:/ai-teacher/yuesheng-writing-coach/src/renderer/hooks/useRightPanel.ts) — hook
- [SettingsPanel.module.css](file:///d:/ai-teacher/yuesheng-writing-coach/src/renderer/styles/SettingsPanel.module.css) — 样式
