# FB20260617-008 RWR-P0-4 项目 IPC + 项目表 migration

> 任务类型：A 类 / 阻塞链（P0） / 中型 📦
> 前置：RWR-P0-1（021_teaching_progress.sql 已用 021 编号）
> 后置：RWR-P0-5（数据迁移脚本）/ RWR-P1-3（ProjectSelector 消费 project:* 通道）

## 1. 任务概述

按 RWR-P0-4 规格交付项目域基础设施：projects 表 + 5 个 IPC 通道 + 类型定义 + handler 实现。

### DoD 验收

| DoD | 结果 |
|-----|------|
| projects 表结构: id/name/description/settingTree/settingTreeType/createdAt/updatedAt | ✅ |
| project:* 通道完整 (list/create/update/delete/get) | ✅ |
| 合约类型在 api-contracts/project.contract.ts | ✅ |
| IPC 通道命名遵循 domain:action 格式 | ✅ (project:list 等 5 个) |
| tsc 0 errors | ✅ |
| vitest 167 passed (9 files) | ✅ |
| 单文件 ≤ 300 行 | ✅ (project.contract 119, project.handler 137) |

## 2. 关键决策

### 2.1 迁移文件编号冲突
- 规格：021_projects.sql
- 冲突：021_teaching_progress.sql 已存在
- 验证：项目用自定义迁移系统（`app-initializer.ts:191-260`），按 name（完整文件名）做 PRIMARY KEY 去重，**021 前缀允许重复**
- 决策：保留 021_projects.sql，**不动 P0-1**

### 2.2 Contract 文件名单/复数
- 规格：project.contracts.ts（多 s）
- 现状：13 个现有 contract 全部单数（session.contract.ts / chat.contract.ts / manuscript.contract.ts）
- 决策：project.contract.ts（单数，与现状一致）

### 2.3 IPC 通道命名
- 规格内部矛盾：行 72 说 `project:*` vs 行 81 说 `contract:` 前缀
- 现状规范：所有 50+ 通道用 `domain:action` 格式（session:list / manuscript:create / chat:send）
- 决策：project:list / project:create / project:get / project:update / project:delete（与 session/manuscript 一致）

### 2.4 外键策略
- 规格未明确
- 依赖图：P0-4 → P0-5
- 决策：P0-4 只建 projects 表，sessions/manuscripts.project_id 外键由 P0-5 处理

### 2.5 时间字段类型
- 规格未明确
- 现状：021_teaching_progress 用 INTEGER + unixepoch()
- 决策：INTEGER + unixepoch()（与 021_teaching_progress 一致）

## 3. 变更详情

### 3.1 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `src/main/db/021_projects.sql` | 31 | projects 表 + idx_projects_updated_at |
| `src/shared/api-contracts/project.contract.ts` | 119 | ProjectInfo + 5 Request/Response + ProjectApi |
| `src/main/ipc/project.handler.ts` | 137 | 5 个 handler + rowToProjectInfo 投影函数 |

### 3.2 修改文件

| 文件 | 变更 | 用途 |
|------|------|------|
| `src/main/core/app-initializer.ts` | +1 行 | migrationFiles 列表添加 '021_projects.sql' |
| `src/main/core/ipc-registry.ts` | +5 行 | import + initProjectHandlers + registerProjectHandlers |
| `src/shared/api-contracts/index.ts` | +11 行 | 项目域桶导出 |
| `src/shared/constants.ts` | +11 行 | IPC_CHANNELS 5 个 + ALLOWED_INVOKE_CHANNELS 5 个 |

### 3.3 表结构

```sql
CREATE TABLE projects (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT DEFAULT NULL,
  setting_tree TEXT DEFAULT NULL,           -- JSON
  setting_tree_type TEXT NOT NULL DEFAULT 'main',
  created_at INTEGER NOT NULL DEFAULT (unixepoch()),
  updated_at INTEGER NOT NULL DEFAULT (unixepoch())
);
CREATE INDEX idx_projects_updated_at ON projects(updated_at DESC);
```

### 3.4 5 个 IPC 通道

| Channel | 动作 | 防御性 |
|---------|------|--------|
| `project:list` | SELECT all ORDER BY updated_at DESC | 数组投影 |
| `project:get` | SELECT WHERE id | null check |
| `project:create` | INSERT + crypto.randomUUID() | required name |
| `project:update` | 动态 SET 拼接 | PROJECT_NOT_FOUND if 0 changes |
| `project:delete` | DELETE | PROJECT_NOT_FOUND if 0 changes |

## 4. R-030 10 步执行记录

| 步骤 | 操作 | 结果 |
|------|------|------|
| Step -1 | 预分类 A 类 / P0 / 中型 | ✅ |
| Step 0 | 查重（无重复） | ✅ |
| Step 1 | 溯源 + 5 项决策 | ✅ |
| Step 2 | 拆分 F-001~F-004 | ✅ |
| Step 3 | 用户确认（5 项决策） | ✅ |
| Step 3.5 | tag v1.0.0-pre-FB20260617-008 | ✅ |
| Step 4 | F-001~F-003 执行 | ✅ |
| Step 4.5 | tsc/vitest 验收 | ✅ |
| Step 5 | DoD 7 项全过 | ✅ |
| Step 5.5 | commit 942dd05 + tag v1.0.0-post | ✅ |
| Step 6 | 报告归档 + TASK-CHAIN V17.1 | ✅ |

## 5. 双向绑定状态同步

| 状态源 | 内容 | 状态 |
|--------|------|------|
| TASK-CHAIN 当前指针 | RWR-P0-4 → RWR-P0-5 | 待推进 |
| IPC_CHANNELS | +5 个 project:* | ✅ |
| ALLOWED_INVOKE_CHANNELS | +5 个白名单 | ✅ |
| 5 消费者（ProjectSelector） | 待实现 (P1-3) | 未启动 |

## 6. 阻塞链更新

```
P0-1 ✅ ─→ P0-2 ✅ ─→ P0-3 ✅ ─→ P0-4 ✅ ─→ P0-5 (待启动)
                              │
                              └→ P1-3 (ProjectSelector 依赖 project:* 通道)
```

## 7. 经验教训

### 7.1 Trae Edit 工具缓存问题
- **症状**：Edit 显示成功（cat 输出确认有改动），但实际文件未持久化
- **根因**：Trae IDE 内部缓存与文件 I/O 不同步
- **解决**：用 Python 脚本直接操作文件，绕过 Edit 工具
- **影响**：本任务中 ipc-registry.ts import 行的修改需要 Python 重做

### 7.2 规格自检
- 规格文件（REWRITE-TASK-DETAILS.md）内部存在矛盾（`project:*` vs `contract:`）
- 决策时优先遵循"现状规范"而非"规格原样"
- 但用户有最终决定权（决策 1 用户选择规格原样 021）

## 8. 关联文档

- [REWRITE-TASK-DETAILS.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/REWRITE-TASK-DETAILS.md) — RWR-P0-4 规格
- [PROJECT-DEPENDENCY-GRAPH.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/PROJECT-DEPENDENCY-GRAPH.md) — 依赖图
- [2026-06-17-system-rewrite-spec.md](file:///d:/ai-teacher/yuesheng-writing-coach/dev-docs/designs/2026-06-17-system-rewrite-spec.md) — 系统重写规格
- [TASK-CHAIN.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/TASK-CHAIN.md) — 任务链（V17.1 推进）
