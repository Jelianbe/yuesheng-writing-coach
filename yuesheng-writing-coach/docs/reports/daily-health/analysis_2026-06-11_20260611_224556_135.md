---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: f2ac1166b9bdaea71c23aabee181e425_49428cb465a411f1b1ba5254002afed2
    ReservedCode1: uFQFzLzo/hX+2f8xv9LSZoGPxyK67VULc23SJwSO3rSjgvkE5vohAxJtaUhzhKxJDkIVGRTcr2afaTC81gnzXB6ziQC8aMRKTrq8dSKJMgROEPrb8/3QYQyg1kMWqSOr6hi34J4/oJeIAqeLjZ6yKp3H23VfOYZxMq/iVTD6JLEV2683L9WC0QvoJfE=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: f2ac1166b9bdaea71c23aabee181e425_49428cb465a411f1b1ba5254002afed2
    ReservedCode2: uFQFzLzo/hX+2f8xv9LSZoGPxyK67VULc23SJwSO3rSjgvkE5vohAxJtaUhzhKxJDkIVGRTcr2afaTC81gnzXB6ziQC8aMRKTrq8dSKJMgROEPrb8/3QYQyg1kMWqSOr6hi34J4/oJeIAqeLjZ6yKp3H23VfOYZxMq/iVTD6JLEV2683L9WC0QvoJfE=
---

# 每日代码体检报告 2026-06-11

## 总体评估

- 健康度：🟡 需关注
- P0: 5 / P1: 6 / P2: 8
- 对比基线：首次以 V2.1 标准审查（之前为 V2.0 简化版）

---

## 阶段 0：脚手架扫描

| 指标 | 今日 |
|------|:----:|
| 总 .ts/.tsx 文件数 (src/) | 124 |
| 总行数 | 21,597 |
| 超 300 行红线 (R-019) | 16 |
| 超 500 行禁止 (R-019) | 8 |
| Q1 console 残留 | 23 |
| Q2 @ts-ignore/expect-error | 6 |
| Q3 `: any` 类型 | 27 |
| Q4 `as unknown as` 类型绕过 | 7 |
| Q5 TODO/FIXME 标记 | 3 |
| Q6 `return []/{}/null` 空返回 | 58 |
| Q7 `readFileSync/writeFileSync` | 21 |
| Q8 `process.env.` 直接引用 | 2 |
| Q9 `dangerouslySetInnerHTML` | 0 |
| Q10 `as` 类型断言 | 288 |
| 构建状态 (tsc --noEmit) | ✅ 0 错误 |

### 大文件 Top 10

| 行数 | 文件 | R-019 状态 |
|-----:|------|:----------:|
| 955 | `src/renderer/shared/types.d.ts` | 🚫 超 500（豁免: .d.ts） |
| 909 | `src/main/services/teaching-strategy-router.ts` | 🚫 超 500 |
| 708 | `src/main/services/student-model.service.ts` | 🚫 超 500 |
| 654 | `src/main/services/__tests__/evidence.service.test.ts` | 🚫 超 500（豁免: 测试） |
| 648 | `src/main/ipc/chat.handler.ts` | 🚫 超 500 |
| 553 | `src/main/services/teaching-state-machine.ts` | 🚫 超 500 |
| 537 | `src/main/ipc/__tests__/full-flow.wiremock.test.ts` | 🚫 超 500（豁免: 测试） |
| 529 | `src/main/services/__tests__/state-locking.test.ts` | 🚫 超 500（豁免: 测试） |
| 442 | `src/renderer/stores/__tests__/training.store.test.ts` | ⚠️ 超 300（豁免: 测试） |
| 379 | `src/main/services/teaching-strategy.service.ts` | ⚠️ 超 300 |

---

## P0 致命问题（SLA: ≤1 工作日）

| ID | 类型 | 文件:行号 | 代码片段 | 问题 | 修复建议 |
|----|------|----------|---------|------|---------|
| P0-001 | sync_io_block | `src/main/ipc/chat.handler.ts:72` | `diagnosisPrompt = fs.readFileSync(promptPath, 'utf-8')` | `callDiagnosisAgent` 在 IPC handler 内同步读取文件，阻塞 Electron 主进程事件循环。该函数被 chat:send handler 调用，每次用户发消息都会触发。 | 改为 `fs.promises.readFile` 或 `fs.readFile` 回调版；或提前在初始化时预加载 prompt 字符串缓存 |
| P0-002 | db_autoincrement | `src/main/db/018_db_p1a_time_format.sql:18` | `id INTEGER PRIMARY KEY AUTOINCREMENT` | 项目标准使用 TEXT PRIMARY KEY (UUID)，但 018 迁移中的 `teaching_state_p1a` 临时表使用自增整数主键。虽然该表是迁移中间产物会被重命名覆盖，但若迁移中途崩溃将留下不符合标准的数据。 | 迁移表也使用 TEXT PRIMARY KEY + UUID 作为 `id`；或在迁移脚本末尾显式 DROP 临时表 |
| P0-003 | handler_no_trycatch | `src/main/ipc/ability-profile.handler.ts:26-41` | `ipcMain.handle(IPC_CHANNELS.ABILITY_GET_PROFILE, async (_event, args) => { ... })` | 使用原始 `ipcMain.handle` 而非项目标准的 `createHandler` 工厂。虽然有手动 try-catch，但仅返回 `apiError(String(error))`——错误消息直接暴露 `error.toString()`，可能泄漏堆栈信息。同时与其他 handler（diagnosis/training 用 createHandler）不一致。 | 统一使用 `createHandler` 工厂；或至少确保 `error.message` 脱敏 |
| P0-004 | channel_consistency | `src/main/ipc/ability-profile.handler.ts:28` + `src/main/ipc/chat.handler.ts` | 两个文件各自用 `ipcMain.handle` 注册，diagnosis.handler / training.handler 用 `createHandler` 工厂 | IPC handler 注册方式不统一：ability-profile 和 chat 用原始 `ipcMain.handle`，其他 handler 用 `createHandler`。`createHandler` 有统一错误格式 `{success, data, error}`，但 `ipcMain.handle` 返回 `apiSuccess/apiError` 格式。renderer 端解析结果的方式因此不一致。 | 全部迁移到 `createHandler` 工厂 |
| P0-005 | db_no_transaction | `src/main/db/*.sql` (全部 18 个迁移文件) | 零个 `BEGIN` / `COMMIT` / `ROLLBACK` | 数据库迁移脚本全部未使用显式事务。SQLite 每条语句默认自动提交，多语句迁移若中途失败会导致部分应用、部分未应用的状态，数据不一致。 | 每个迁移文件外层包裹 `BEGIN TRANSACTION; ... COMMIT;`，并在文件头增加 `PRAGMA journal_mode=WAL;` 或至少在迁移框架层统一开启事务 |

---

## P1 功能风险

| ID | 类型 | 文件:行号 | 问题 | 修复建议 |
|----|------|----------|------|---------|
| P1-001 | cp_in_preload | `src/preload/index.ts:7-18` | `contextBridge.exposeInMainWorld('electronAPI', { invoke, on })` 的 `invoke` 方法包含同步白名单检查 `ALLOWED_INVOKE_CHANNELS.includes(channel)`。如果通道名不在数组中，直接抛出 Error 而不返回 Promise.reject——可能导致 renderer 侧未 catch 的同步异常。 | 改为 `return Promise.reject(new Error(...))` 或将检查逻辑移到 `ipcMain.handle` 端验证 |
| P1-002 | async_mismatch | `src/main/ipc/chat.handler.ts:69` | `async function callDiagnosisAgent(...)` 内调用 `fs.readFileSync`——async 函数内有同步阻塞 I/O，让 async 标记失去意义 | 改用 `await fs.promises.readFile(...)` |
| P1-003 | error_exposure | `src/main/ipc/create-handler.ts:32` | `console.error(\`[IPC] ${channel} error:\`, error)` —— `createHandler` 工厂在 catch 块直接 console.error 完整 error 对象，生产环境可能输出堆栈到终端 | 生产环境下只 log `error.message`，堆栈仅开发环境输出 |
| P1-004 | store_fetch_no_error | `src/renderer/stores/chapter.store.ts:171` | `fetchByWork(manuscriptId)` 在 store 内部被 await 调用但未检查返回值 `success` 字段（createHandler 返回 `{success, data}` 而 ipcMain.handle 返回 `apiSuccess/apiError` 格式） | 统一 IPC 返回格式后，在 store 侧检查 `result.success` |
| P1-005 | useEffect_no_cleanup | `src/renderer/components/` (5 处 useEffect) | 5 个组件使用 useEffect，需逐项检查是否有订阅/定时器未清理 | 逐一审查，确认返回 cleanup 函数 |
| P1-006 | readFileSync_in_services | `src/main/services/` (21 处) | 21 个 `readFileSync/writeFileSync` 调用，虽然不在 IPC 线程直接调用，但在 Electron main process 中仍会阻塞 | 批量迁移为 `fs.promises` 版本（除启动初始化阶段外） |

---

## P2 可维护性

| ID | 类型 | 文件:行号 | 问题 | 建议 |
|----|------|----------|------|------|
| P2-001 | any_type | 6 文件 27 处 | 27 处 `: any` 类型标注 | 逐个替换为 unknown / 泛型 / 具体联合类型 |
| P2-002 | ts_ignore | 5 文件 6 处 | 6 处 `@ts-ignore` / `@ts-expect-error` | 逐一审查，确认是否可以消除（用类型守卫替代） |
| P2-003 | as_unknown_as | 4 文件 7 处 | 7 处 `as unknown as T` 双重断言，绕过 TypeScript 类型系统 | 检查上游类型定义，从根源修正而非绕过 |
| P2-004 | empty_return | 31 文件 58 处 | 58 处 `return []` / `return {}` / `return null` | 区分"正常空结果"与"异常空结果"；异常情况应 throw 或返回 error |
| P2-005 | todo_stale | 3 文件 3 处 | `feedback-engine.ts:100`、`student-classifier.ts:75`、`writing-analyzer.ts:82` 三个 TODO | 标注截止日期或转为 TASK-CHAIN 任务 |
| P2-006 | console_residue | 6 文件 23 处 | console.log/warn/error 残留（含 createHandler 工厂内的 1 处 + ability-profile 内的 2 处） | 引入统一 logger 服务，支持分级输出和开发/生产环境切换 |
| P2-007 | big_service | `teaching-strategy-router.ts` (909行) | 超 500 行红线，需拆分 | 按策略类型拆分为子路由（WorldBuilder / CharacterBuilder / PacingBuilder 等） |
| P2-008 | big_handler | `chat.handler.ts` (648行) | 超 500 行红线，诊断 Agent 调用逻辑和消息路由逻辑混在同一文件 | 抽离 `callDiagnosisAgent` 为独立 service 文件 |

---

## P3 架构风险（下个架构迭代）

| ID | 风险类型 | 模块 | 描述 | 瓶颈等级 |
|----|---------|------|------|:--------:|
| A-001 | 大文件集中 | teaching-strategy-router.ts (909行), student-model.service.ts (708行), chat.handler.ts (648行), teaching-state-machine.ts (553行) | 核心业务 4 个文件合计 2,818 行，占全项目 13% | ⚠️ 中 |
| A-002 | IPC 返回格式分裂 | ability-profile.handler + chat.handler vs 其他 handler | ipcMain.handle 返回 `{success, data, errorCode}` (apiSuccess格式) vs createHandler 返回 `{success, data, error}`——两种格式混用，renderer 端需同时兼容两种解析路径 | ⚠️ 中 |
| A-003 | 类型断言泛滥 | 63 文件 288 处 `as` 断言 | 平均每文件 2.3 处类型断言，仅占文件总数的 50%，但集中在复杂 service 文件和 IPC 层 | 🔵 低 |
| A-004 | process.env 裸引用 | `app-initializer.ts:28`, `window-manager.ts:22` | 环境变量直接散落在源码中，缺少集中配置管理 | 🔵 低 |

---

## 趋势对比

> 首次 V2.1 审查，无历史对比数据。以下为基线记录。

| 指标 | 今日 |
|------|:----:|
| 总行数 | 21,597 |
| 大文件(>300行) | 16 |
| console残留 | 23 |
| @ts-ignore 数 | 6 |
| any 类型数 | 27 |
| as 断言数 | 288 |
| P0 问题数 | 5 |
| 构建错误 | 0 |

---

## 反糊弄声明

- 逐文件验证：扫描了 src/ 下全部 124 个 .ts/.tsx 文件
- IPC 通道验证：对比了 ALLOWED_INVOKE_CHANNELS (41 通道) vs 10 个 handler 文件的注册列表，发现 ability-profile 和 chat 两个 handler 未使用 createHandler 工厂
- DB 检查：读取了全部 18 个迁移 .sql 文件，发现零事务包裹 + 1 处 AUTOINCREMENT
- 安全扫描：检查了硬编码密钥（0 发现）、.env 文件（不存在）、shell.openExternal（0 使用）
- 未扫描的文件：无（全量扫描）
- P0=5 不为 0，无需额外解释声明
*（内容由AI生成，仅供参考）*
