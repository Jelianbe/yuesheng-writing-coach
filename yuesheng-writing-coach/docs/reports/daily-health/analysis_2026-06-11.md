---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: f2ac1166b9bdaea71c23aabee181e425_9b9804ff65a011f195cd525400d9a7a1
    ReservedCode1: 597kQOjE4q8Qk+dlNKoQNAfNp0+nky4MFqWMDeQUW/giqhzsoCi7Bu8L5ccTbv11d5AZiE5h/FMHa4gpN8fK0A+htSTgMDi2/ZDecT8OZ1rSVpnO9ZRMCOS3GT/hU3SsVKpprKBcELKy1TxzIBvBVx24uyws71+faTF4oljC9nGh/DWp40guuYmMwB8=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: f2ac1166b9bdaea71c23aabee181e425_9b9804ff65a011f195cd525400d9a7a1
    ReservedCode2: 597kQOjE4q8Qk+dlNKoQNAfNp0+nky4MFqWMDeQUW/giqhzsoCi7Bu8L5ccTbv11d5AZiE5h/FMHa4gpN8fK0A+htSTgMDi2/ZDecT8OZ1rSVpnO9ZRMCOS3GT/hU3SsVKpprKBcELKy1TxzIBvBVx24uyws71+faTF4oljC9nGh/DWp40guuYmMwB8=
---

# 项目体检报告 - 2026-06-11

> **日期**：2026-06-11 | **项目**：月笙写作教练 (yuesheng-writing-coach)
> **扫描范围**：`src/` 目录，177 个文件，27,859 行
> **审查清单**：SYSTEM_HEALTH_CHECK_PROMPT_V1.0.md
> **扫描脚本**：`scripts/daily_health_scan.py`

---

## 🔴 P0 致命问题：2 个（需立即修复）

### 1. Broken Import：`paradigm.store.ts` → `TabBar`

| 项目 | 内容 |
|------|------|
| **文件** | `src/renderer/stores/paradigm.store.ts:12` |
| **import 语句** | `import type { ParadigmATabId } from '../components/layout/TabBar'` |
| **问题** | 目标路径 `src/renderer/components/layout/TabBar` 不存在。该目录下无 `TabBar.ts` / `TabBar.tsx` 文件 |
| **影响** | 类型导入失败，编译时 TypeScript 报错，`ParadigmATabId` 类型无法解析 |
| **建议** | 确认 TabBar 组件是否已移除/重命名/移动。如已移至 `SessionTabBar.tsx`，修改 import 路径为 `../components/layout/SessionTabBar`；如该类型已不必要，移除 import |

### 2. Broken Import：`app-helpers.ts` → `RightPanel`

| 项目 | 内容 |
|------|------|
| **文件** | `src/renderer/utils/app-helpers.ts:8` |
| **import 语句** | `import type { RightPanelProps } from '../components/panels/RightPanel'` |
| **问题** | 目标路径 `src/renderer/components/panels/` 目录不存在 |
| **影响** | `RightPanelProps` 类型导入失败，`buildRightPanelSteps()` / `buildRightPanelDiagnoses()` / `buildRightPanelNextStep()` 等函数类型依赖断裂 |
| **建议** | 确认 RightPanel 组件当前实际位置（可能在 `components/layout/RightDrawer.tsx` 或已重构），修正 import 路径 |

---

## 一、P0 审查详情与误判分析

### 真实 P0：2 个
（见上方 🔴 区域）

### 误判分析（4 个误报）

| 序号 | 文件 | 误判类型 | 原因 |
|------|------|----------|------|
| 1 | `right-panel.actions.ts` → `../../stores/right-panel.actions` | broken_import | import 语句位于 JSDoc 注释块（`*   import { rightPanelActions } from '../../stores/right-panel.actions';`），非真实代码 |
| 2 | `diagnosis.handler.ts` | ipc_no_channel | 通过 `createHandler()` 工厂函数注册 IPC 通道，共调用 5 次 `createHandler(IPC_CHANNELS.*)`，通道已正常注册 |
| 3 | `training.handler.ts` | ipc_no_channel | 通过 `createHandler()` 工厂函数注册 IPC 通道，共调用 8 次 `createHandler(IPC_CHANNELS.TRAINING_*)` |
| 4 | `validate-payload.ts` | ipc_no_channel | 纯工具函数文件，不负责 IPC 通道注册，不属于 Handler |

**误判根因**：扫描脚本的 IPC 检测正则仅匹配 `ipcMain.handle/on` 字面量，未识别 `createHandler()` 工厂模式。`validate-payload.ts` 不应归入 Handler 分类。

---

## 二、大文件拆分评估

### Top 10 超过 300 行的文件

| 排名 | 文件 | 行数 | 拆分建议 |
|------|------|------|----------|
| 1 | `src/renderer/shared/types.d.ts` | 955 | 🟡 类型声明文件，建议按模块拆分为 `types/main.d.ts` / `types/renderer.d.ts` / `types/teaching.d.ts` 等 |
| 2 | `src/main/services/teaching-strategy-router.ts` | 909 | 🔴 **优先拆分**：策略路由器逻辑集中，建议按策略类型拆为独立 Strategy 文件，路由只做分发 |
| 3 | `src/main/services/student-model.service.ts` | 708 | 🟡 学生模型服务，考虑将评分函数、能力矩阵计算、历史分析独立为子模块 |
| 4 | `src/main/ipc/chat.handler.ts` | 648 | 🟡 聊天 IPC 处理，建议将消息预处理、流式响应、上下文管理拆为独立 concern |
| 5 | `src/main/services/teaching-state-machine.ts` | 553 | 🟡 状态机定义 + 转换逻辑，建议 State/Transition/Action 分离 |
| 6 | `src/renderer/components/layout/WorkTreePanel.tsx` | 403 | 🟢 可接受范围内，后续迭代拆分子面板 |
| 7 | `src/renderer/components/manuscript/ManuscriptPanel.tsx` | 386 | 🟢 可接受范围内 |
| 8 | `src/main/services/teaching-strategy.service.ts` | 379 | 🟢 可接受范围内 |
| 9 | `src/main/services/dynamic-context.service.ts` | 363 | 🟢 可接受范围内 |
| 10 | `src/main/services/prompt-loader.ts` | 356 | 🟢 可接受范围内 |

**统计**：10 个文件超过 300 行，无超过 1000 行的文件。项目规模健康。

---

## 三、类型安全审计

### 3.1 `any` 类型使用

| 指标 | 数值 |
|------|------|
| 全项目 any 总数 | **9** |
| 含 any 的文件数 | 7 |
| any 密度（每万行） | 3.2 |

**详细分布**：

| 文件 | any 数 | 备注 |
|------|--------|------|
| `session.service.test.ts` | 3 | 测试文件，可接受 |
| `ipc-registry.ts` | 1 | 待审查 |
| `prompt-loader.ts` | 1 | 待审查 |
| `student-model.service.ts` | 1 | 待审查 |
| `OnboardingFlow.tsx` | 1 | 待审查 |
| `electron.d.ts` | 1 | 类型声明文件，通常需要 any |
| `ipc.ts` | 1 | 待审查 |

**评价**：any 使用量极低（9 个 / 2.8 万行），类型安全意识优秀。建议对生产代码中的 5 个 any（排除 test 和 .d.ts）逐一审查是否可替换为 `unknown` 或具体类型。

### 3.2 `@ts-ignore`

| 指标 | 数值 |
|------|------|
| @ts-ignore 总数 | **1** |
| 所在文件 | `src/renderer/plugins/cjs-to-esm-fix.ts` |

**评价**：近乎零 `@ts-ignore`，优秀。

### 3.3 `as` 类型断言

| 指标 | 数值 |
|------|------|
| as 断言总数 | **242** |
| 密度（每百行） | 0.87 |

**评价**：断言密度 0.87/百行，属中等水平。Electron 项目中 IPC 序列化常需要类型断言，属正常现象。建议后续扫描对 `as any` 模式做专项统计。

---

## 四、代码质量指标

| 指标 | 数值 | 评级 |
|------|------|------|
| console.log/warn/error 调用 | 102 | 🟡 中等 |
| TODO 标记 | 5 | 🟢 优秀 |
| FIXME 标记 | 0 | 🟢 优秀 |
| 循环依赖 | 未扫描 | — |

### console.log 分布分析

102 个 console 调用分布在项目中。Electron 主进程使用 console 是标准做法（渲染进程日志通过 IPC 转发），但渲染进程中的 `console.log` 建议迁移到统一日志服务。

---

## 五、DB 事务与 IPC 通道完整性

### DB 分析

- `src/main/db/` 目录仅有 2 个 `.sql` 文件（`013_manuscripts.sql`、`018_db_p1a_time_format.sql`），无 TypeScript 文件
- 实际的数据库操作逻辑分布在 `src/main/services/` 中（通过 `better-sqlite3`）
- **建议**：下版扫描脚本增加对 `better-sqlite3` 的 `transaction()` / `exec()` / `prepare()` 调用的检测，确保事务覆盖率

### IPC 通道完整性

- 13 个 IPC 目录下的文件，全部通过 `createHandler()` 工厂模式注册
- `create-handler.ts` 内部调用 `ipcMain.handle()` 并包裹 try-catch 和标准化返回格式
- **评价**：IPC 架构设计干净，工厂模式统一了错误处理和返回值格式，架构质量高
- **改进建议**：`validate-payload.ts` 属工具函数，建议移出 `ipc/` 目录到 `main/utils/` 避免分类混淆

---

## 六、趋势对比

**基准建立**：本次为首次每日体检扫描，建立基线数据如下：

| 指标 | 基线值 (2026-06-11) |
|------|---------------------|
| 总文件数 | 177 |
| 总行数 | 27,859 |
| P0 问题 | 2（均为 broken import） |
| 大文件（>300行） | 10 |
| any 使用 | 9 |
| @ts-ignore | 1 |
| as 断言 | 242 |
| console 调用 | 102 |
| TODO | 5 |
| FIXME | 0 |

> 后续每日扫描将自动与基线对比，生成趋势变化。

---

## 七、修复建议汇总

| 优先级 | 数量 | 类别 | 建议行动 |
|--------|------|------|----------|
| 🔴 P0 | 2 | broken import | 修正 `paradigm.store.ts` 和 `app-helpers.ts` 的 import 路径 |
| 🟡 P1 | 2 | 大文件拆分 | `teaching-strategy-router.ts`(909行) 和 `student-model.service.ts`(708行) 本周内拆分 |
| 🟡 P1 | 1 | 目录整理 | `validate-payload.ts` 从 `ipc/utils/` 移至 `main/utils/` |
| 🟢 P2 | 5 | any 类型 | 审查生产代码中 5 个 any 是否可优化 |
| 🟢 P2 | 1 | 扫描增强 | 下版扫描脚本增加 `better-sqlite3 transaction()` 检测和 `as any` 专项统计 |

---

*报告生成时间：2026-06-11 | 审查清单版本：SYSTEM_HEALTH_CHECK_PROMPT_V1.0*
*（内容由AI生成，仅供参考）*
