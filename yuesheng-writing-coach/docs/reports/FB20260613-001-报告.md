# FB20260613-001：新手引导流程重构 + 空对话布局修复

> 反馈分类: D（功能设计） | 严重度: P1
> 日期: 2026-06-13

---

## Step -1：预分类

- **分类**：D — 功能设计与 UI 改造
- **判定理由**：用户主动提出的功能改造需求，涉及启动流程变更、设置入口整合、布局修复三个维度
- **A 类豁免判定**：不适用，涉及多处文件改动和状态管理变更

## Step 0：查重结果

- **TASK-CHAIN 搜索关键词**：onboarding / ConfigPage / SettingsPanel / 布局
- **是否命中**：部分（T-019 从零构建引导已覆盖 OnboardingFlow 组件，但本次为流程重构，非增量修改）
- **处理方式**：F-01（写作前七问引导）因面向中高级用户暂缓；T-019 的输出（OnboardingFlow 组件）作为本任务的输入
- **反馈编号**：FB20260613-001

## Step 1：溯源报告

### 问题定义
- **标题**：新手引导流程重构 + 空对话布局修复
- **分类**：D
- **严重度**：P1
- **影响范围**：App.tsx、ChatView.tsx、MessageInput.tsx、RightDrawer.tsx、ConfigPage.tsx（移除路由）

### 溯源四环（R-018）
1. **设计哲学依据**：新手引导应"先问需、后配置"，让新用户先完成身份确认再进入主界面；配置入口整合到右侧抽屉，消除独立全屏页面
2. **技术规格依据**：`FULLSTACK_REFACTOR_PLAN.md` Phase 3~4 已完成 Service 层解耦，ConfigPage 不再作为必经路由
3. **研究基础依据**：当前 ConfigPage 全屏显示导致"返回按钮失效"问题已多次修复（2026-06-12~13），根本原因是独立路由与 AppConfigGate 两套机制冲突。本次将其统一到 SettingsPanel 消除冲突
4. **前序任务衔接**：
   - T-019 OnboardingFlow → 直接复用已有组件
   - Phase 3.2 App.tsx 瘦身 → App.tsx 当前 152 行，本次进一步精简
   - RIGHT_PANEL_MAPPING (2026-06-12) → SettingsPanel 已具备完整配置项

### Skill 关联
- Step -1 预分类：brainstorming
- Step 1 溯源：brainstorming + search
- Step 2 拆分：brainstorming
- Step 4 执行：general_purpose_task
- Step 5 验收：general_purpose_task

## Step 2：原子化拆分

| 子任务 | 描述 | 预计规模 | DoD（≥3 条） |
|--------|------|---------|-------------|
| F-001 | App.tsx 流程重构：移除 view='config' 路径、ConfigPage 导入、重新编排启动逻辑 | S | 1. view='config' 路径移除 2. ConfigPage 导入/引用删除 3. 启动流程改为 ready→isNewUser→onboarding→main |
| F-002 | ChatView 空状态欢迎卡片 + flex 布局修复：添加未配置 API Key 时的引导提示，输入框贴底 | M | 1. 空状态欢迎卡片居中显示 2. 输入框 flex-shrink:0 贴底 3. 无 API Key 时卡片显示引导文案 |
| F-003 | MessageInput 禁用逻辑：无 API Key 时发送按钮置灰 + hover 提示 | XS | 1. isConfigured 注入 2. 发送按钮 disabled 样式 3. hover title 提示 |
| F-004 | RightDrawer 齿轮红点角标：未配置时齿轮图标带红点提示 | XS | 1. isConfigured 读取 2. 红点 CSS 角标 3. 配置后自动消失 |
| F-005 | ConfigPage 清理：从 App.tsx 移除渲染路径，文件保留 | XS | 1. App.tsx 删除导入和 JSX 2. 无其他文件引用报错 |

### 三大原则检验
- [x] **收尾确认**：前序 Phase 3~4 全栈模块化重构已完成，AppConfigGate 已解除配置拦截
- [x] **基础提供**：SettingsPanel 已具备完整 API 配置表单项（Key/URL/Model/Temperature/态度模式）
- [x] **自然衔接**：从当前 App.tsx（ready→AppConfigGate→onboarding→main）改为（ready→isNewUser→onboarding→main→SettingsPanel）

### 冲突检测（5 项）
- [x] 与已完成任务无矛盾
- [x] 与待执行任务无重叠
- [x] 不违反界面改造边界声明
- [x] 不属于"影子功能"反模式
- [x] 前置依赖已就绪（OnboardingFlow 组件、SettingsPanel、config.store 均已完成）

## Step 3：用户确认

- [x] **批准** → Step 3.5

## Step 3.5：Git 备份

- [x] 备份分支: `backup/pre-FB20260613-001` (commit `5f0c064`)

## Step 4：执行结果

| 子任务 | 描述 | 状态 |
|--------|------|------|
| F-001 | App.tsx 流程重构 — 移除 ConfigPage 路由、精简 store destructure、调整启动流程 | ✅ |
| F-002 | ChatView 空状态欢迎卡片 + flex 布局修复 — 输入框贴底 | ✅ |
| F-003 | MessageInput 禁用逻辑 — 无 API Key 时发送按钮置灰 + title 提示 | ✅ |
| F-004 | RightDrawer 齿轮红点角标 — 未配置时显示 | ✅ |
| F-005 | ConfigPage 清理 — 从 App.tsx 移除渲染路径，文件保留 | ✅ |

### 验证结果

- `tsc --noEmit` — ✅ 0 errors
- `vitest run` — ✅ 136/136 passed

## Step 5：验收

- [x] 类型检查通过
- [x] 测试全通过
- [x] Dev server HMR 无报错

## Step 6：闭环

- [x] 报告闭环
- [ ] 待用户确认后更新 decision-log.md
