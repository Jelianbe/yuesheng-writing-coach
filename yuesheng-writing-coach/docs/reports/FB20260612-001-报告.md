# FB20260612-001：集成 Function Calling (Tool Use)

> 反馈分类: B | 严重度: P1
> 日期: 2026-06-12

---

## Step -1：预分类

- 分类：**B**（架构变更）
- 判定理由：涉及核心流式响应架构扩展，新增 tool-call round-trip 循环，但以「新增独立入口不修改原有函数」为原则，风险可控
- A 类豁免判定：≥5 行且涉及 IPC/状态管理，不豁免

## Step 0：查重结果

- TASK-CHAIN 搜索关键词：`function calling`、`tool use`、`工具调用`
- 是否命中：否（全新功能）
- 反馈编号：FB20260612-001

---

## Step 1：溯源报告

### 问题定义
- 标题：集成 Function Calling 替代正则章节引用方案
- 分类：B
- 严重度：P1
- 影响范围：`chat.handler.ts`、`api-proxy.ts`、`App.tsx`

### 溯源四环（R-018）
1. **设计哲学依据**：当前 `resolveChapterReference` 用应用层正则替换 `/chapters/{uuid}`，用户必须用精确格式说话，AI 不知道自己在使用工具。标准做法是 API 原生 Function Calling。
2. **技术规格依据**：[PLAN-FUNCTION-CALLING.md V2](../plans/PLAN-FUNCTION-CALLING.md)
3. **研究基础依据**：OpenAI/DeepSeek 均支持 `tools` 参数的流式 tool_calls
4. **前序任务衔接**：`resolveChapterReference` 已实现但仅支持精确匹配，用户反馈"不管发什么都会回复旧章节内容"（PR 级别：当前已修复正则灵活性）

### Skill 关联
- Step -1 预分类：plan-review-expert（V2 方案已迭代两轮自评）
- Step 1 溯源：architecture-review-expert
- Step 2 拆分：plan-review-expert
- Step 4 执行：modern-js-async-expert
- Step 5 验收：test-automation-expert

---

## Step 2：原子化拆分

| 子任务 | 描述 | 预计规模 | DoD（≥3 条） |
|--------|------|---------|-------------|
| F-001 | `api-proxy.ts`：新增 `getBaseUrl()`/`getApiKey()` getter + `accumulateToolCalls()` + `chatStreamWithTools()` | XS(~50行) | 1. getter 暴露给 probeToolSupport 使用 2. `accumulateToolCalls` 精确累积 chunk 3. `chatStreamWithTools` 能区分 text/tool_calls 两种事件 |
| F-002 | `chat.handler.ts`：新增 `toolHandlers` (readChapter) + `handleStreamResponseWithTools` | S(~100行) | 1. readChapter 支持 chapterId 精确查找 + titleHint 模糊匹配 + 无参返回近期列表 2. `handleStreamResponseWithTools` 处理 tool-call 循环（MAX=3） 3. 中断/错误处理覆盖 |
| F-003 | `chat.handler.ts`：新增 `probeToolSupport` + 调度入口选择 | XS(~40行) | 1. 白名单/黑名单/未知模型 test call 三层兼容性探测 2. 调度层根据结果选择入口 3. 未支持模型降级到原有 `handleStreamResponse` |
| F-004 | `App.tsx`：新增 `chat:tool:executing` 监听 + 工具调用状态提示 | XS(~15行) | 1. 收到事件显示状态条 2. 3秒自动清除 3. 不阻塞用户操作 |

### 三大原则检验
- [x] 收尾确认：前序任务（resolveChapterReference 灵活性修复）已完成
- [x] 基础提供：本任务为后续扩展 searchKnowledge 等工具提供基础设施
- [x] 自然衔接：从 PLAN-FUNCTION-CALLING.md V2 的具体实现方案开始

### 冲突检测（5 项）
- [x] 与已完成任务矛盾？否 — 新增独立入口，不修改现有函数
- [x] 与待执行任务重叠？否
- [x] 违反界面改造边界声明？否 — 后端改动为主，前端仅加一条状态提示
- [x] 属于"影子功能"反模式？否 — 已出完整方案文档
- [x] 前置依赖未就绪？否 — 所有依赖就绪

---

## Step 3：用户确认

- [x] 批准 → Step 3.5
- [ ] 修改 → 返回 Step 2
- [ ] 暂不执行 → decision-log.md 记录
- [ ] 取消 → 终止

## Step 3.5：Git 备份

- [x] 创建备份分支 `backup/pre-FB20260612-001`
- [x] 备份分支包含当前工作区全部修改

## Step 4：执行编码

| 子任务 | 状态 | 涉及文件 | 关键变更 |
|--------|:----:|---------|---------|
| F-001 | ✅ 完成 | `src/main/api-proxy.ts` | 新增 `getBaseUrl()`/`getApiKey()` getter、`accumulateToolCalls()`、`chatStreamWithTools()` AsyncGenerator |
| F-002 | ✅ 完成 | `src/main/ipc/chat.handler.ts` | 新增 `toolHandlers.readChapter`、`TOOLS_DEFINITIONS`、`handleStreamResponseWithTools()`（含 MAX=3 轮循环）|
| F-003 | ✅ 完成 | `src/main/ipc/chat.handler.ts` | 新增 `probeToolSupport()`（三层策略：白名单/黑名单/test call）、调度入口选择 |
| F-004 | ✅ 完成 | `src/shared/constants.ts`、`src/preload/index.ts`、`src/main/ipc/chat.handler.ts`、`src/renderer/App.tsx` | 新增 `CHAT_TOOL_EXECUTING` 通道常量+白名单、`chat:tool:executing` 前台监听、工具调用状态条 |

### 附带修复
- `chat.handler.ts`：移除 `handleStreamResponseWithTools` 未使用的 `diagnosisAnalysis`/`isNarrative` 参数
- `full-flow.wiremock.test.ts`：调整断言适应新工具流 `chat:stream:data` 调用顺序变化

## Step 4.5：代码质量审查（R-027 四道门禁）

| 门禁 | 结果 | 验证方式 |
|:----:|:----:|---------|
| Gate 1: tsc | ✅ 0 errors | `npx tsc --noEmit` |
| Gate 2: vitest | ✅ 136 passed | `npx vitest run` |
| Gate 3: 循环依赖 | ✅ 无 | `npm run check:circular` |
| Gate 4: build | ✅ 成功 | `npm run build` |

## Step 5：验收

### 自动化验收
- [x] tsc 0 errors
- [x] 全部 136 测试通过
- [x] 循环依赖检查通过
- [x] 生产构建成功

### 人工验收清单
- [x] 功能正确性：`handleStreamResponseWithTools` 为独立新入口，不修改原有 `handleStreamResponse`
- [x] 无回归：原测试全部通过
- [x] IPC 通信安全：新通道已加入 preload 白名单 + ALLOWED_EVENT_CHANNELS
- [x] 前端体验：工具调用状态条 3 秒自动清除，不阻塞用户操作
- [x] R-019 规范：无 `any`、无 `@ts-ignore`、具名导出、camelCase 命名
- [x] R-021 最小范围：只修改必要文件，无顺手格式化和重构
- [x] R-028 防御性编码：`toolHandlers` 函数有 try/catch，参数校验通过 `chapter:get` IPC

## Step 6：闭环更新

- [x] TASK-CHAIN.md：新增 V16.9 变更记录
- [x] 报告归档：Step 4~6 记录已补充
- [x] 备份分支保留：`backup/pre-FB20260612-001` 保留至下一反馈周期

## Step 7：验收签字

- **功能完整性**: ✅ 全部 F-001~F-004 完成
- **代码质量**: ✅ 四道门禁通过
- **回归风险**: ✅ 无
- **文档关联**: ✅ 方案文档 PLAN-FUNCTION-CALLING.md + 本报告
- **签字**: ✅ 2026-06-12 用户确认通过
