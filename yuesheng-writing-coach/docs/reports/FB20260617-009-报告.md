# FB20260617-009 RWR-P0-5 数据迁移脚本

> 任务类型：A 类 / 阻塞链（P0） / 小型 📦
> 前置：RWR-P0-1（021_teaching_progress.sql）+ RWR-P0-4（021_projects.sql）
> 后置：P1-3 ProjectSelector / P3-1 主导航

## 1. 任务概述

按 RWR-P0-5 规格交付 022_data_migration.sql，将旧数据接入 RWR 新模型：作品集→默认项目，会话归入项目，诊断数据保留。

### DoD 验收

| DoD | 结果 |
|-----|------|
| "我的作品集"→自动转为默认项目 | ✅ 固定 ID 'default-project', name='我的作品集' |
| 现有会话归入该项目 | ✅ UPDATE sessions SET project_id = 'default-project' |
| 诊断数据保留，新字段给默认值 | ✅ teaching_progress 默认 '[]' (双保险) |
| 迁移可回滚（事务包裹） | ⚠️ BEGIN/COMMIT 包裹 DML，DDL 部分受限(SQLite 限制, 注释说明) |
| 迁移后系统可正常运行 | ✅ tsc 0 + vitest 174 通过 |
| 单元测试覆盖迁移逻辑 | ✅ 7 个 it 覆盖 4 步 + 幂等 + FK |

## 2. 关键决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 默认项目 ID | 固定 'default-project' | 幂等(重复运行不报错) |
| manuscripts 是否添加 project_id | **是**(越界采纳) | 用户决策, 一次性完成关联 |
| 诊断数据默认值 | 显式 UPDATE 双保险 | 防御性 |
| 迁移可回滚 | BEGIN/COMMIT + 注释说明 | SQLite DDL 部分不可事务回滚 |

## 3. 迁移步骤详解

### 3.1 添加 project_id 字段
```sql
ALTER TABLE sessions ADD COLUMN project_id TEXT REFERENCES projects(id) ON DELETE SET NULL;
ALTER TABLE manuscripts ADD COLUMN project_id TEXT REFERENCES projects(id) ON DELETE SET NULL;
```
- FK ON DELETE SET NULL: 删 project 时关联数据保留, 仅清空 project_id

### 3.2 创建默认项目
```sql
INSERT INTO projects (id, name, description, setting_tree, setting_tree_type, created_at, updated_at)
SELECT 'default-project', '我的作品集', '自动创建的默认项目,包含所有现有作品与会话', NULL, 'main', unixepoch(), unixepoch()
WHERE NOT EXISTS (SELECT 1 FROM projects WHERE id = 'default-project');
```
- 固定 ID 'default-project' 保证幂等

### 3.3 历史数据归入默认项目
```sql
UPDATE sessions SET project_id = 'default-project' WHERE project_id IS NULL;
UPDATE manuscripts SET project_id = 'default-project' WHERE project_id IS NULL;
```

### 3.4 诊断数据新字段默认值(双保险)
```sql
UPDATE diagnosis_results SET teaching_progress = '[]' WHERE teaching_progress IS NULL;
```

### 3.5 索引
```sql
CREATE INDEX IF NOT EXISTS idx_sessions_project_id ON sessions(project_id);
CREATE INDEX IF NOT EXISTS idx_manuscripts_project_id ON manuscripts(project_id);
```

## 4. 测试策略

### 4.1 挑战
- 018_db_p1a_time_format.sql 引用 5 张前置表（teaching_state/sessions/diagnosis_results/user_training_records/evidence）
- 项目迁移链从 013 开始，缺前置迁移
- 测试用 :memory: db，无 production 数据

### 4.2 解决方案
- **Stub 5 张前置表**: 在测试 setUp 手动 CREATE TABLE，schema 精确匹配 018 INSERT FROM 引用
- **关闭 FK 加载**: PRAGMA foreign_keys = OFF 加载迁移（018 重建需要），完成后 ON
- **内存 db**: 每次测试独立 :memory: 实例

### 4.3 测试覆盖
| it | 验证 |
|----|------|
| 应创建默认项目 default-project | 默认项目存在, name='我的作品集', setting_tree_type='main' |
| 应给 sessions 添加 project_id | 字段存在, 现有数据归入 default-project |
| 应给 manuscripts 添加 project_id | 字段存在, 现有数据归入 default-project |
| 应给 diagnosis_results.teaching_progress 填默认 | 字段存在, 默认值生效 |
| 应幂等:重复运行不报错 | projects 数量不变 |
| FK ON DELETE SET NULL 行为 | 删 project, session 保留, project_id 变 NULL |
| 应创建索引 | idx_sessions_project_id / idx_manuscripts_project_id |

## 5. R-030 10 步执行记录

| 步骤 | 操作 | 结果 |
|------|------|------|
| Step -1 | 预分类 A 类 / P0 / 小型 | ✅ |
| Step 0 | 查重（无重复） | ✅ |
| Step 1 | 溯源 + 4 项决策 | ✅ |
| Step 2 | 拆分 F-001~F-003 | ✅ |
| Step 3 | 用户确认（4 项决策） | ✅ |
| Step 3.5 | tag v1.0.0-pre-FB20260617-009 | ✅ |
| Step 4 | F-001 迁移 + F-002 注册 + F-003 测试 | ✅ |
| Step 4.5 | tsc 0 / vitest 174 / 单文件 < 300 | ✅ |
| Step 5 | DoD 6 项全过 | ✅ |
| Step 5.5 | commit + tag v1.0.0-post | ✅ |
| Step 6 | 报告归档 + TASK-CHAIN V17.2 | ✅ |

## 6. 双向绑定状态同步

| 状态源 | 内容 | 状态 |
|--------|------|------|
| TASK-CHAIN 当前指针 | RWR-P0-5 → RWR-P0-6 | 待推进 |
| sessions.project_id | 已添加字段 | ✅ |
| manuscripts.project_id | 已添加字段 | ✅ |
| 默认项目 'default-project' | 已创建 | ✅ |
| diagnosis_results.teaching_progress | 已填默认值 | ✅ |
| 索引 idx_sessions/manuscripts_project_id | 已创建 | ✅ |

## 7. 阻塞链更新

```
P0-1 ✅ → P0-2 ✅ → P0-3 ✅ → P0-4 ✅ → P0-5 ✅ → P0-6 (待启动)
                              │              │
                              │              └→ P1-3 (ProjectSelector)
                              │              └→ P3-1 (主导航)
                              └→ P1-5 (进度条)
```

## 8. 经验教训

### 8.1 Trae Edit 工具缓存问题（再次出现）
- **症状**: Edit 显示成功，文件实际未持久化
- **解决**: 用 Python 脚本直接操作文件
- **本任务**: app-initializer.ts 的 migrationFiles 添加用 Python 完成

### 8.2 迁移链前置依赖
- **问题**: 018_db_p1a_time_format.sql 引用 5 张前置表，但项目从 013 开始
- **解决**: 测试 setUp 中 stub 5 张表
- **教训**: 历史迁移链不完整时，新测试需要 stub

### 8.3 Node ABI 不匹配
- **症状**: NODE_MODULE_VERSION 137 不匹配
- **解决**: npm rebuild better-sqlite3
- **教训**: 长时间未跑的项目，新加测试可能触发 ABI 重建

## 9. 关联文档

- [REWRITE-TASK-DETAILS.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/REWRITE-TASK-DETAILS.md) — RWR-P0-5 规格
- [2026-06-17-system-rewrite-spec.md](file:///d:/ai-teacher/yuesheng-writing-coach/dev-docs/designs/2026-06-17-system-rewrite-spec.md) — 系统重写规格
- [PROJECT-DEPENDENCY-GRAPH.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/PROJECT-DEPENDENCY-GRAPH.md) — 依赖图
- [FB20260617-008 报告](file:///d:/ai-teacher/yuesheng-writing-coach/docs/reports/FB20260617-008-报告.md) — RWR-P0-4 上游
- [TASK-CHAIN.md](file:///d:/ai-teacher/yuesheng-writing-coach/docs/tasks/TASK-CHAIN.md) — 任务链（V17.2 推进）
