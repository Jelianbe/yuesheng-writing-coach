# Product

> **代际校订（2026-09-17）**：本文档原稿沿用早期 **ELE（Electron + React）** 一代的平台假设
> ——「左侧栏 / 右侧栏」「`prefers-reduced-motion`」「键盘可达」。这些描述与当前
> **手机 Android 竖屏唯一**的平台事实冲突（该形态在竖屏下不成立），已按 Flutter 真源逐处改写。
>
> **产品定位部分（Users / Purpose / Brand Personality / Anti-references / Design Principles 1-2-3-5）
> 未经改动**——经代码核实仍有效：「轻一点」安全词降档见
> `yuesheng-flutter/lib/config/shared_constants.dart:435`；态度档位 doubao→yuesheng→sensei 见
> `yuesheng-flutter/lib/services/attitude_advisor.dart:25`。
> 早期 ELE 一代的版本保留在 `yuesheng-writing-coach/PRODUCT.md`（归档，勿据其推断当前形态）。

## Register

product

## Users

长篇小说写作者，独立创作。打开月笙时通常处于两种心智状态之一：
- **写作中**：沉浸于情节推进，需要随时记录、贴入作品、不被 AI 打断节奏
- **复盘时**：写完一段需要诊断问题、获得训练建议、看到自己进步

工作环境：单人多 session，可长时间保持应用开启（数小时），需要快速进入/退出写作心流。

## Product Purpose

月笙是写作教练 + 成长记录器，不是代写工具。核心链路是：
**用户作品 → 发现问题 → 解释原因 → 制定训练 → 执行训练 → 验证进步 → 形成能力**

存在意义：让写作者从"反复写同一个问题"到"系统性突破"。

成功标志：用户回头看历史问题时，能看到自己某类问题（比如开篇）从「频繁出现」到「不再出现」。

## Brand Personality

**静水深流**——初看低调不抢眼，相处越久越让人信住。

3 词：**内敛 / 严谨 / 持久**

不是：廉价的"加油你真棒"，不是冷冰冰的纠错机，不是工具栏的快捷键集合。

像一位资深编辑：不会抢你笔，但会指出你看不到的盲点；不会粉饰问题，但也不会让你丧失信心。

## Anti-references

- **不要做成 AI 助手风格**（豆包/ChatGPT 亮色+表情+鼓励语），会冲淡"教练"的严肃感
- **不要做成 SaaS 工具栏**（Linear/Notion 边栏堆满），会冲淡"写作心流"——写作是主要内容，UI 应退到背景
- **不要堆砌动效**（macOS Big Sur 弹跳 + 卡片翻转），会让长时使用疲劳
- **不要用渐变色 + 玻璃拟态**（glassmorphism 装饰化），会显得轻浮
- **不要 emoji 当 UI 元素**（⭐️ 📚 🎯），会冲淡专业感
- **不要"小红点 + 数字徽章"过载**，会制造焦虑

## Design Principles

1. **内容为主，UI 退到背景**：写作区域是主舞台，所有辅助界面（诊断流、训练流、覆盖式抽屉）应该"让出"主舞台的呼吸空间
2. **不替写、不替决定**：永远把判断权交回给用户。诊断给"事实+证据"，训练给"路径+对比"，不直接覆盖用户原文
3. **进度可见、可回溯**：每一次诊断/训练/复盘都该是事件流的清晰节点，用户能"翻看"自己的成长
4. **安全词机制**：「轻一点」无条件降档，态度从 yuesheng/sensei 降为 doubao（更温柔）。这是人格底线，不是功能开关
5. **配置外置、避免硬编码**：教学法/技法/症候/五步流都是 JSON 配置可改，不写死在代码里

## Accessibility & Inclusion

- **WCAG 2.1 AA**：所有文本对比 ≥4.5:1，大字 ≥3:1
- **减弱动效**：遵循系统「移除动画」无障碍设置（Flutter 侧读 `MediaQuery.disableAnimations`），所有动画必须有降级版本（瞬间切换或淡入淡出）
- **触屏可达 + 读屏友好**：所有交互（Tab 切换、训练步骤、抽屉操作）点击区达 44dp 下限，且能被 TalkBack 识别并朗读
- **焦点可达**：若接外接键盘，所有交互（Tab 切换、训练步骤、抽屉操作）可纯键盘完成
- **焦点环可见**：不依赖颜色区分状态，必须有焦点环/下划线/形状变化
- **中文优先**：界面文案中文为主，技术术语保留英文（如 "training flow" "core subset"）
- **不留白屏 / 死页**：每个面板/tab 必须有"功能开发中"占位 + 返回入口
