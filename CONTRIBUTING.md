# 贡献指南

> 面向**人类贡献者**与**AI 协作者**。本文件只回答两件事：**改什么**、**改到什么程度算完成**。
> 它**不复述规则条文，也不复述任何会随版本漂移的数字**（门禁道数、用例数、覆盖率……）——
> 那些一律指向各自的可执行真源。**复述即漂移。**

---

## 一、先读

> **本文件是自足的**：外部贡献者读完本文件即可完成贡献，**无需访问任何内部文件**。
> 下文提到的 `AGENTS.md` 与 `.ai/` 是**仓库维护者与 AI 协作者**的内部真源，**不在公开仓内**。
> 若你能读到它们，说明你已在项目内部环境中工作 —— 此时它们**优先于本文件**（更详细、更新更快）。

| 档 | 读什么 | 什么时候 |
|:--|:--|:--|
| 30 秒 | 下面这段**最高条款** | 任何时候，先读这个 |
| 5 分钟 | **本文件全文** | 动手前 |
| 内部环境 | `AGENTS.md`（AI 协作入口）· `.ai/README.md`（项目库读写协议） | 需要「现在在哪 / 什么已作废 / 口径是什么」时 |

**最高条款**：本产品是写作**教学**系统，不是写作工具 ——
**不替用户写句子，不替用户做决定**。任何与之冲突的「帮用户省事」的改动，一律让位。

---

## 二、三层边界（先判断你的改动属于哪一层）

本项目把经常被混在一起的三样东西显式拆开。**动手前先归层**：

| 层 | 判据 | 典型例子 | 违反后果 |
|:--|:--|:--|:--|
| **约束层** | **有脚本能拦** | 函数行数上限 · 循环依赖 · 密钥扫描 · 覆盖率 · prompt 反模式 | 门禁红灯，**拦得住** |
| **约定层** | 有客观判据但无脚本；或纯工作方式 | 提交信息格式 · 文档分类 · 调试记录 · 过程声明 | **不拦** —— 必须在文中写明「这是约定，不是门禁」 |
| **项目库** | 是**记忆**不是规则 | 现在在哪 / 什么作废 / 口径 | — |

> ### ★ 硬纪律
> **任何一条「规则」若声称自己是硬约束，必须指向一个可执行检查。**
> 指不出来 ⇒ 它必须搬进**约定层**并明确标注无机器执行点。
> 这是终结「装作有约束」的唯一办法。

**各层的真源位置**：

| 层 | 真源 | 是否随公开仓分发 |
|:--|:--|:--:|
| 约束层 | `yuesheng-flutter/scripts/gate.sh`（脚本本体 + 头部注释） | ✅ |
| 约定层 | **本文件** | ✅ |
| 规则本体 | `.trae/rules/`（有独立版本控制，**不在公开仓内**） | ❌ |
| 规则索引与裁定 | `.ai/rules/README.md`（标注每条是否真有机器执行点） | ❌ |
| 项目库 | `.ai/`（独立仓） | ❌ |

---

## 三、贡献路径

### 3.1 改代码

- **唯一活跃真源 = `yuesheng-flutter/`**（Flutter + Dart + Riverpod + drift）。所有开发在这里。
- 其余顶层目录**一律不代表当前产品**：`yuesheng-writing-coach/`（已归档的旧一代 Electron 实现）、
  `historical-archive/`（旧设计稿 —— **设计稿 ≠ 实现**）、`release-apk/` 等。
  ⚠️ 引用任何架构或 UI 结论前，**先判代际**。本项目最容易犯的认知错误就是拿旧一代的结论当现状。
- 改完必须跑门禁（见 §四）。**门禁道数与逐道参数以 `gate.sh` 头部注释为准**，本文件不复述。

### 3.2 改规则或约定

- **规则本体**在 `.trae/rules/`（独立仓，有版本控制）。改规则 = 改那个仓，**不是**改本文件。
- 新增或修改任何「声称是硬约束」的条目时，**必须同时给出机器执行点**；
  给不出 ⇒ 写进本文件的§六**约定层**，并如实标注无机器执行点。
- 涉及 **prompt / Skill 内容**的改动属高风险：改注入 prompt = **改变 AI 行为**，需人工确认后再提交。

### 3.3 改文档

- 面向人类的门面文档在仓库根（`README.md` / 本文件 / `SECURITY.md` / `CHANGELOG.md`）。
- **不要在本仓任何文档里复述会漂移的数字**（门禁道数、用例数、基线条数、覆盖率）。
  一律写成「以 `<脚本/文件>` 实测为准」。动因：权威文档复述了会变的数字，
  而文档本身不受脚本约束，脚本改了它不会自动同步 —— **复述即漂移**。
- 内部分析类文档（审计 / 设计 / 报告）落在项目库与内部目录，**按设计不进公开仓**。

---

## 四、完成判据（DoD）

**唯一真源**：`yuesheng-flutter/scripts/gate.sh`。一键跑全部：

```bash
cd yuesheng-flutter
bash scripts/gate.sh
```

- **声称「完成」前必须门禁全绿**，并**粘贴命令输出**，而不仅是说「测试通过了」。
- 日常迭代可用快道（`scripts/gate-fast.sh`，不产覆盖率产物）；
  **收尾一律用收尾道**（`gate.sh`）。快道全绿**不等于**收尾全绿。
- 覆盖率类门禁依赖完整测试运行产物：**不要用窄范围测试产生的产物去喂收尾道**，会得到假红灯。
- 需真实 API Key 的用例由 wrapper 固定排除（`live` / `external` 标签），**不要为了跑绿而删除这些标签**。

---

## 五、提交规范

- **按 pathspec 提交**，不要 `git add -A` / `git add .` —— 本仓存在多套并行内容与忽略规则，
  全量暂存会把不相干的东西卷进来。
- 提交信息：单行主题（含类型前缀）+ 分条正文（用 ①②③ 分条，说明**为什么**而不是复述 diff）。
- 改动范围遵守**最小化**：不顺手格式化 / 重构任务范围外的文件。
- 推送后用 `git ls-remote origin <ref>` 核实远端，**不要依赖本地 tracking ref**
  （本仓已知环境异常：`refs/remotes/**` 写入会静默失效）。
- 涉及不可逆操作（删除、批量重命名、`reset --hard`）前，先建可回退路径。

---

## 六、约定层（**无机器执行点** —— 如实声明）

以下内容**没有脚本拦得住**，属于团队工作约定。把它们当硬约束会导致

> 「以为被拦住了，实际没有」

| 约定 | 说明 |
|:--|:--|
| 提交信息格式 | 见 §五。无 commitlint hook，靠人守 |
| 文档分类与命名 | 门面文档放根；审计/设计/报告放内部目录 |
| 调试记录 | 修 bug 时留下归因过程（根因、判据、反例），不只留 diff |
| 过程声明 | 未验证的功能必须**明确声明「未验证」**，不得含糊 |
| 假设驱动 | 涉及核心模块（诊断 / Skill 注入 / 教学状态机 / DB schema）先写 ADR |

---

## 七、禁止事项

> 以下 **8 条是完整清单，不是摘要**。它们是不随版本变化的**原则**；
> 各自的**机器执行点**见 §二 的约束层（真源 = `yuesheng-flutter/scripts/gate.sh`）。
> ⚠️ 其中**未被脚本覆盖的条目没有机器拦截**，靠代码审查与自觉 —— 这点如实声明，不含糊。

1. 不替用户写句子或做创作决定（最高条款）
2. 不顺手格式化 / 重构任务范围外的文件
3. 不在 Widget 内直接操作全局状态（须经 Riverpod Provider）
4. 不硬编码 API Key 或敏感配置
5. 不新增未经论证的静态映射表
6. 不引入当前未使用的新框架或第三方库
7. 不用 `part` / `extension` 机械拆分服务层凑行数 —— 属**伪拆分**，已有回退先例
8. 服务层超行数上限的拆分须走 ADR 决策，不得直接动手

---

## 八、报告问题

- **功能缺陷 / 使用问题**：开 issue，附复现步骤、期望行为、实际行为。
- **安全漏洞**：**不要开公开 issue** —— 见 `SECURITY.md`。
- **行为准则问题**：见 `CODE_OF_CONDUCT.md`。

---

## 许可

贡献即表示同意以本项目的 `LICENSE`（MIT）条款授权你的贡献。
