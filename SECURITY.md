# 安全策略（Security Policy）

## 支持范围

本项目处于**活跃开发**阶段。安全修复只针对**默认分支的最新状态**；
历史提交与旧版安装包不提供安全支持。

---

## 报告漏洞

> ### ⚠️ 请勿为安全漏洞开公开 issue

**首选渠道**：GitHub 私密漏洞报告 —— 仓库页 **Security → Report a vulnerability**
（`https://github.com/Jelianbe/yuesheng-writing-coach/security/advisories/new`）。
该渠道仅在维护者与报告者之间可见。

**如果该入口不可用**：开一个 issue，**只写「需要安全的漏洞报告渠道」**，
**不要包含任何漏洞细节**；维护者会回复私密联系方式。

报告时建议包含：

| 项 | 说明 |
|:--|:--|
| 受影响版本 / commit | 便于定位 |
| 复现步骤 | 最小可复现路径 |
| 影响 | 攻击者能做什么（读数据 / 提权 / 泄露凭证……） |
| 概念验证 | 有则附，无则说明 |

**我们的承诺**：收到报告后确认接收；修复后在 `CHANGELOG.md` 记录（**不含利用细节**，直到修复可用）。

---

## 威胁模型（**本项目不是典型 SaaS**）

理解攻击面之前，先理解这个项目的形态，否则会误判风险等级：

| 项 | 事实 | 后果 |
|:--|:--|:--|
| **架构** | **纯本地，无后端中转** —— 项目自身不含服务端 | **无「厂商批量数据泄露」这一类风险** |
| **凭证** | **BYOK**（用户自带 API Key，自行配置） | 不存在项目方持有的主密钥；泄露面在**设备端** |
| **数据** | 稿件、诊断结果存于**本地 SQLite（drift）** | 不上传；风险限于设备本身被攻破 |
| **出网** | 仅向**用户自己配置的 LLM 端点**发请求 | 端点可信度由用户选择，项目不做代理 |

⇒ 因此本项目的安全重点**不是**「保护服务器」，而是：
**① 别把用户的 Key 写进代码 / 日志 / 提示词；② 别让本地数据在用户不知情时外流。**

### 已知的攻击面

| 面 | 风险 | 现有缓解 |
|:--|:--|:--|
| **API Key 存储** | 用户 Key 被读取 | 经 `flutter_secure_storage`（平台安全存储）读写，非明文 SharedPreferences |
| **密钥硬编码 / 入库** | 提交即永久泄露（git history 不可逆） | 门禁第 4 道 `scripts/check_secrets.sh` 在**本地门禁与 CI 双端**阻断 |
| **日志 / 错误上报** | Key 或稿件经日志外流 | 禁止打印完整 Key（必要时仅前 4 位或掩码）；错误经统一处理器脱敏 |
| **Prompt 内容** | 真实 Key 被写进 Skill 常量 | 运行时注入，不写进 prompt 常量 |
| **依赖供应链** | 第三方包引入风险 | 依赖锁定（见 `AGENTS.md`）；新增第三方库需论证 |

---

## 贡献者须知（强制）

- **绝不提交**任何真实 API Key / Token / 密码 —— 包括示例、测试夹具、注释、文档。
- 敏感配置一律经安全存储读写接口，**不要**落到普通偏好存储或明文文件。
- 日志与错误上报中**不得出现完整凭证**。
- 新增任何涉及网络、存储、鉴权的代码，须经安全相关门禁；门禁道数与参数以
  `yuesheng-flutter/scripts/gate.sh` 头部注释为**唯一真源**（本文件不复述）。
- 发现**已提交的**凭证：**立即轮换该凭证**，然后再处理历史 ——
  仅从历史中删除**不足以**恢复安全（git 历史可能已被克隆）。

---

## 不在范围内

- 用户自行选择的第三方 LLM 服务端的安全问题（请报告给该服务方）
- 用户设备已越狱 / root 后的本地数据提取
- 用户主动把自己的 API Key 公开分享
- 社会工程学攻击

---

## 相关文件

| 主题 | 位置 |
|:--|:--|
| 安全与隐私规则本体 | `.trae/rules/R-029-安全与隐私.md` |
| 密钥扫描门禁 | `yuesheng-flutter/scripts/check_secrets.sh` |
| 贡献流程 | `CONTRIBUTING.md` |
| 行为准则 | `CODE_OF_CONDUCT.md` |
