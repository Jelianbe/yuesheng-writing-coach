# 更新日志

> 格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
> 版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
> **版本号真源 = `yuesheng-flutter/pubspec.yaml`**，本文件不复述它。

---

## 关于本文件的起点（请先读）

本文件**自 2026-09-18 起启用**。在此之前项目已积累大量变更，**我们不回溯补写这段历史**。

理由：补写出来的「历史」**无法验证**，而无法验证的复述在后续会话里会**被当作事实引用**——
这是本项目反复踩到的坑（把会漂移的内容写进文档，文档本身却不受脚本约束，于是必然漂移）。

需要更早的变更记录时，**以 git 提交记录为准**：

```
git log --oneline --date=short
```

本文件只记录**从启用之日起**、**对使用者有可感知影响**的变更。
内部重构、测试补强、工具改动等不影响用户的内容不在此列（它们在提交记录里）。

---

## [未发布]

### 新增

- **训练自评档位**：训练完成后可对本次训练做四档**回忆难度**自评
  （再来一次 / 有点难 / 还行 / 很轻松），用于调整该症候的后续复习间隔。

### 说明

- 训练自评与既有的**三维掌握自评**是**两件不同的事**，不互相替代：
  - 回忆难度自评 → 影响**复习节奏**
  - 三维掌握自评 → 提供**掌握证据**（影响评估是否放行）
- 不做自评时，行为与既有版本**完全一致**（不强制、不阻断）。

---

## 版本对应

本项目的发布形态为**安装包**（非库），因此 `CHANGELOG` 的章节与
`yuesheng-flutter/pubspec.yaml` 中的 `version` 一一对应。
两者冲突时，**以 `pubspec.yaml` 为准**。
